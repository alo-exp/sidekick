OBJECTIVE: Add bulk archive workflow: API endpoint, UI control, and two verification scripts.

CONTEXT:
- Notes app with existing CRUD, filters, export/import may exist.
- node_modules installed. Do NOT run npm install.
- Use `cat /tmp/patch.txt | apply_patch` for edits.

DESIRED STATE:
1. POST /api/notes/bulk-archive — body `{ "ids": [1,2,3] }` sets archived=1 for each; 400 if ids missing/invalid; returns `{ "archived": n }`
2. UI: "Archive selected" for multi-select checkboxes on note list (add checkboxes if missing)
3. scripts/verify-bulk-archive-api.sh — curl test
4. scripts/verify-bulk-archive-ui.sh — grep-based UI test

UI ELEMENT REQUIREMENTS (verify-bulk-archive-ui.sh greps exactly):
- index.html: element id `bulkArchiveButton`
- notes-ui.js: wire `bulkArchiveButton`, render `note-checkbox`, track `selectedIds`, reference `bulk-archive` endpoint

SUCCESS CRITERIA:
- Both verify scripts exit 0.
- No code after module.exports.
- Final message:
  STATUS: SUCCESS
  FILES_CHANGED: [...]
  TESTS_RUN: scripts/verify-bulk-archive-api.sh scripts/verify-bulk-archive-ui.sh
  ASSUMPTIONS: []

INJECTED SKILLS: testing-strategy, usability

CORRECTION (prior run incomplete — kay exited 0 without deliverables):
- Complete the full task in this session: POST /api/notes/bulk-archive, UI multi-select + Archive selected button, scripts/verify-bulk-archive-api.sh and scripts/verify-bulk-archive-ui.sh.
- Place new routes BEFORE the /:id catch-all. No code after module.exports.
- In verify-bulk-archive-api.sh: PORT=3457; export PORT; export NOTES_APP_DB_PATH; start server; curl tests; must exit 0.
- Run both verify scripts before stopping.
- apply_patch hunks must end with a line exactly: *** End Patch
- Your final assistant message MUST end with this block (last lines):
STATUS: SUCCESS
FILES_CHANGED: [...]
TESTS_RUN: scripts/verify-bulk-archive-api.sh scripts/verify-bulk-archive-ui.sh
ASSUMPTIONS: []
