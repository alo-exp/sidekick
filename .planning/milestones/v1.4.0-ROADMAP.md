# Milestone v1.4.0: Command-Surface Cleanup & Security Hardening

**Status:** ✅ SHIPPED 2026-04-25
**Phases:** None (direct release — no formal GSD phases used)
**Total Plans:** 0 (changes made directly in a single session)
**GitHub Release:** https://github.com/alo-exp/sidekick/releases/tag/v1.4.0

## Overview

v1.4.0 is a housekeeping release. The milestone scope is entirely command-surface cleanup — no new delegation behavior was introduced. The changes align command names with actual user-facing invocation patterns, fix two security issues in `install.sh`, and remove a command (`/forge-replay`) that relied on a Forge API that is no longer available.

All changes were made directly in a single session on 2026-04-25.

## Changes Delivered

| Change | Files | Notes |
|--------|-------|-------|
| Rename skill `forge-delegation` → `forge-delegate` | `skills/forge/SKILL.md` | Dash naming convention for Claude Code skills |
| Create `/forge-stop` command | `commands/forge-stop.md` | Dedicated deactivation command; discoverable, testable |
| Remove `/forge-replay` command | `commands/forge-replay.md` deleted | Underlying `forge conversation dump --html` API removed from Forge |
| Normalize `/forge-history` (dash) everywhere | 9 files | Was `/forge:history` (colon form) in 9 places — all fixed |
| Remove `curl \| bash` from `install.sh` | `install.sh` | SENTINEL blocking: unsigned remote execution |
| Add `chmod 600` on `~/forge/.credentials.json` | `install.sh` | SENTINEL blocking: world-readable credentials |
| Update `forge-progress-surface.sh` footer | `hooks/forge-progress-surface.sh` | `/forge-history` hint instead of removed `/forge:replay` |
| Add Plugin Hooks to reference page sidebar | `docs/help/reference/index.html` | `#forge-hooks` section was unreachable via navigation |
| Update all help-site docs | 5 pages + search.js + docs/index.html | Reflect forge-stop, forge-history, remove forge-replay |
| Bump plugin.json to v1.4.0 | `.claude-plugin/plugin.json` | 5 integrity hashes refreshed |

## Commits

| Hash | Message |
|------|---------|
| ff04e7b | `refactor: rename forge-delegation → forge-delegate, add forge-stop, remove forge-replay` |
| 23b7d7a | `chore(v1.4.0): pre-release quality gate round 1 fixes` |
| f697658 | `docs: add Plugin Hooks to reference page sidebar nav (D9)` |
| 0d4e3e3 | `docs(v1.4.0): generate milestone summary for onboarding` |
| d54cd09 | `docs(release): update WORKFLOW.md with release flow progress` |
| ff0f5db | `docs(release): fix WORKFLOW.md flow log row IDs (all numeric)` |

## Milestone Summary

### Key Decisions

| ID | Decision | Rationale |
|----|----------|-----------|
| CMD-01 | `forge-delegation` → `forge-delegate` | Dash naming convention for Claude Code skills |
| CMD-02 | `/forge-stop` as dedicated command file | Discoverable, testable, consistent with command surface |
| CMD-03 | Remove `/forge-replay` entirely | Underlying API (`forge conversation dump --html`) removed from Forge |
| SEC-01 | Remove `curl \| bash` from `install.sh` | Unsigned remote execution from secondary domain is SENTINEL blocking |
| SEC-02 | `chmod 600` on credentials file | World-readable API keys are a security violation |
| CONV-01 | `/forge-history` not `/forge:history` | Command frontmatter uses dash; colon form was 9-file inconsistency |
| DOC-01 | Add `#forge-hooks` to reference page sidebar | Section existed in content but was undiscoverable via navigation |

### Tech Debt (Pre-existing)

- `skills/forge/SKILL.md`: 342 lines (doc soft limit 300)
- `hooks/forge-delegation-enforcer.sh`: 289 lines (approaching hard limit 300)
- `hooks/lib/enforcer-utils.sh`: 277 lines (approaching hard limit 300)

### Issues Resolved

None (no GitHub issues targeted by this milestone — it was a direct housekeeping sweep).

### Stats

- **Timeline:** 2026-04-25 → 2026-04-25 (1 session)
- **Commits:** 6 (ff04e7b → ff0f5db)
- **Files changed:** ~27 (+165 / -151)
- **Test suites:** 15 suites, 157 assertions, 0 failures
