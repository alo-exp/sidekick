---
phase: 01-spec-core-skill-and-forge-config
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - .forge/agents/forge.md
  - .forge.toml
  - .forge/skills/quality-gates/SKILL.md
  - .forge/skills/security/SKILL.md
  - .forge/skills/testing-strategy/SKILL.md
  - .forge/skills/code-review/SKILL.md
autonomous: true
requirements:
  - FCFG-01
  - FCFG-02
  - FCFG-03
  - FCFG-04

must_haves:
  truths:
    - ".forge/agents/forge.md exists with delegation-awareness content"
    - ".forge.toml exists with exact compaction defaults from D-07"
    - "All 4 bootstrap skills exist under .forge/skills/"
    - "Bootstrap skills use Forge SKILL.md format per D-10 (YAML frontmatter with trigger field)"
    - "No Claude-specific tool names appear in any .forge/ skill file"
  artifacts:
    - path: ".forge/agents/forge.md"
      provides: "Project-level Forge agent override"
      contains: "Sidekick"
    - path: ".forge.toml"
      provides: "Forge configuration with compaction defaults"
      contains: "token_threshold = 80000"
    - path: ".forge/skills/quality-gates/SKILL.md"
      provides: "Quality gates skill for Forge"
      contains: "id: quality-gates"
    - path: ".forge/skills/security/SKILL.md"
      provides: "Security skill for Forge"
      contains: "id: security"
    - path: ".forge/skills/testing-strategy/SKILL.md"
      provides: "Testing strategy skill for Forge"
      contains: "id: testing-strategy"
    - path: ".forge/skills/code-review/SKILL.md"
      provides: "Code review skill for Forge"
      contains: "id: code-review"
  key_links:
    - from: ".forge/agents/forge.md"
      to: "./AGENTS.md"
      via: "standing instruction to read AGENTS.md"
      pattern: "AGENTS.md"
---

<objective>
Implement Forge configuration files: `.forge/agents/forge.md` agent override, `.forge.toml` configuration template, and `.forge/skills/` bootstrap skill set.

Purpose: These files configure Forge to work as a Sidekick-orchestrated agent. Without them, Forge has no delegation awareness, no compaction settings, and no initial skills.
Output: 6 new files in `.forge/` directory tree plus `.forge.toml` at project root.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-spec-core-skill-and-forge-config/01-CONTEXT.md
@.planning/phases/01-spec-core-skill-and-forge-config/01-RESEARCH.md
@.planning/phases/01-spec-core-skill-and-forge-config/01-01-SUMMARY.md
@skills/forge.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .forge/agents/forge.md and .forge.toml</name>
  <files>.forge/agents/forge.md, .forge.toml</files>
  <read_first>
    - .planning/forge-delegation-spec.md (section 11 -- Forge Config File Specs)
    - .planning/phases/01-spec-core-skill-and-forge-config/01-CONTEXT.md (D-06 agent override content, D-07 toml defaults)
    - .planning/phases/01-spec-core-skill-and-forge-config/01-RESEARCH.md (code examples for both files)
    - skills/forge.md (STEP 0A-3 for existing .forge.toml config pattern)
  </read_first>
  <action>
FILE 1: `.forge/agents/forge.md`

Create directory `.forge/agents/` and write this file. It must have YAML frontmatter with:
  id: forge
  title: Forge (Sidekick-Orchestrated)
  description: Default Forge agent with Sidekick delegation awareness
  tools: ["*"]

Body content (exact):

# Forge Agent -- Sidekick Project Override

You are being orchestrated by Claude (Sidekick plugin). Claude is the planner
and communicator; you are the implementer.

## Standing Instructions

1. Read `./AGENTS.md` for project-specific conventions before starting any task
2. Produce structured output: start with what you did, end with what changed
3. If you discover a reusable pattern, note it at the end of your response so
   Claude can add it to AGENTS.md
4. Do not ask questions -- execute the task as specified. If ambiguous, make a
   reasonable choice and document your assumption

## Output Format

At the end of every task, include a block with these exact fields:
  STATUS: SUCCESS | PARTIAL | FAILED
  FILES_CHANGED: [list]
  ASSUMPTIONS: [any assumptions made]
  PATTERNS_DISCOVERED: [reusable patterns for AGENTS.md]

Per D-06: This injects (1) AGENTS.md reference, (2) delegation-mode awareness, (3) structured output format, (4) standing instruction to update AGENTS.md.
Per D-05/FCFG-04: This file will only be created if absent. The SKILL.md (plan 01-02) checks existence before creating.

FILE 2: `.forge.toml`

Create at project root with this exact TOML content:

"$schema" = "https://forgecode.dev/schema.json"
max_tokens = 16384

[compact]
token_threshold = 80000
eviction_window = 0.20
retention_window = 6

[session]
provider_id = "open_router"
model_id = "qwen/qwen3-coder-plus"

Per D-07: These are the exact compaction defaults the user locked.
Per D-05/FCFG-04: This file will only be created if absent.
  </action>
  <verify>
    <automated>test -f .forge/agents/forge.md && test -f .forge.toml && grep -q "token_threshold = 80000" .forge.toml && grep -q "Sidekick" .forge/agents/forge.md && echo "OK" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `.forge/agents/forge.md` exists
    - Contains YAML frontmatter with `id: forge`
    - Contains `tools: ["*"]` in frontmatter (grants Forge agent full tool access — without this, agent is provisioned with zero tools and silently emits fake tool-call text)
    - Contains string "Sidekick" (delegation awareness)
    - Contains string "AGENTS.md" at least twice (reference + standing instruction)
    - Contains "STATUS: SUCCESS | PARTIAL | FAILED" (output format)
    - `.forge.toml` exists at project root
    - Contains exactly: `max_tokens = 16384`
    - Contains exactly: `token_threshold = 80000`
    - Contains exactly: `eviction_window = 0.20`
    - Contains exactly: `retention_window = 6`
    - Contains `provider_id = "open_router"`
  </acceptance_criteria>
  <done>Agent override and TOML config created with exact values from D-06 and D-07</done>
</task>

<task type="auto">
  <name>Task 2: Create .forge/skills/ bootstrap skill set</name>
  <files>.forge/skills/quality-gates/SKILL.md, .forge/skills/security/SKILL.md, .forge/skills/testing-strategy/SKILL.md, .forge/skills/code-review/SKILL.md</files>
  <read_first>
    - .planning/forge-delegation-spec.md (section 8 -- Skill Injection Protocol, section 11 -- bootstrap set)
    - .planning/phases/01-spec-core-skill-and-forge-config/01-CONTEXT.md (D-08 bootstrap set, D-10 format rules)
    - .planning/phases/01-spec-core-skill-and-forge-config/01-RESEARCH.md (Forge SKILL.md format example)
    - skills/forge.md (for understanding what quality/security/testing patterns the project uses)
  </read_first>
  <action>
Create 4 bootstrap skills under `.forge/skills/`. Each MUST follow D-10 format:
- YAML frontmatter with fields: id, title, description, trigger
- Imperative language ("Run X", "Write Y", "Verify Z")
- NO Skill tool references, NO AskUserQuestion calls
- NO Claude-specific tool names (Read, Edit, Write, Bash, Grep, Glob)
- Use generic file operation language instead

SKILL 1: `.forge/skills/quality-gates/SKILL.md`
Frontmatter: id=quality-gates, title=Quality Gates, description=Enforce code quality standards before committing changes, trigger=test lint commit quality review push
Body: 7 imperative rules covering: run test suite, run linter, check TODO/FIXME, verify no debug statements, confirm descriptive names, run full suite before push, verify commit message conventions.

SKILL 2: `.forge/skills/security/SKILL.md`
Frontmatter: id=security, title=Security Review, description=Check for common security issues in code changes, trigger=security auth credential secret password token api-key encrypt
Body: 7 imperative rules covering: no hardcoded secrets, env vars for sensitive values, .gitignore includes credential files, input validation, error messages don't leak internals, file permissions on credentials, no sensitive data in logs.

SKILL 3: `.forge/skills/testing-strategy/SKILL.md`
Frontmatter: id=testing-strategy, title=Testing Strategy, description=Guide test creation and ensure adequate coverage, trigger=test spec coverage unit integration assert expect
Body: 8 imperative rules covering: tests before implementation, one behavior per test, edge cases, bash exit codes, file operation error cases, test independence, run full suite, clear error messages.

SKILL 4: `.forge/skills/code-review/SKILL.md`
Frontmatter: id=code-review, title=Code Review, description=Review code changes for quality consistency and correctness, trigger=review refactor cleanup improve optimize pr
Body: 8 imperative rules covering: change matches objective, consistency with patterns, no duplicated logic, error handling, clear names, no side effects, comments explain why, no broken interfaces.

After creating all 4, verify no Claude-specific terms leaked in:
Run: grep -r "Skill tool\|AskUserQuestion\|Read tool\|Edit tool\|Write tool\|Bash tool" .forge/skills/
This MUST return empty (no matches).
  </action>
  <verify>
    <automated>ls .forge/skills/*/SKILL.md | wc -l | awk '{if ($1 == 4) print "OK: 4 skills"; else print "FAIL: "$1" skills"}' && ! grep -rq "Skill tool\|AskUserQuestion\|Read tool\|Edit tool\|Write tool\|Bash tool" .forge/skills/ && echo "No Claude refs: OK" || echo "FAIL: Claude-specific refs found"</automated>
  </verify>
  <acceptance_criteria>
    - 4 SKILL.md files exist: `.forge/skills/quality-gates/SKILL.md`, `.forge/skills/security/SKILL.md`, `.forge/skills/testing-strategy/SKILL.md`, `.forge/skills/code-review/SKILL.md`
    - Each has YAML frontmatter with `id`, `title`, `description`, `trigger` fields
    - `grep -r "Skill tool" .forge/skills/` returns no matches
    - `grep -r "AskUserQuestion" .forge/skills/` returns no matches
    - `grep -r "Read tool\|Edit tool\|Write tool\|Bash tool" .forge/skills/` returns no matches
    - Each file uses imperative language ("Run", "Write", "Verify", "Check")
    - `bash tests/run_all.bash` still passes (no regression)
  </acceptance_criteria>
  <done>4 bootstrap skills created in Forge-compatible SKILL.md format with no Claude-specific syntax</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .forge/agents/forge.md to Forge agent | Injected system prompt into Forge execution context |
| .forge/skills/ to Forge Skill Engine | Auto-detected skills influence Forge behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Tampering | .forge/agents/forge.md | accept | User reviews agent override; non-destructive creation preserves customizations (D-05) |
| T-01-03-02 | Tampering | .forge/skills/*.md | accept | Bootstrap skills contain generic quality guidance only, no secrets or sensitive logic |
| T-01-03-03 | Spoofing | Forge prompt injection via AGENTS.md ref | accept | Trust Gate already handled in forge.md STEP 2 (SENTINEL FINDING-1.1) |
</threat_model>

<verification>
- `test -f .forge/agents/forge.md` returns 0
- `test -f .forge.toml` returns 0
- `ls .forge/skills/*/SKILL.md | wc -l` returns 4
- `grep -q "token_threshold = 80000" .forge.toml` returns 0
- `grep -rq "Skill tool" .forge/skills/` returns 1 (no match = success)
- `bash tests/run_all.bash` passes
</verification>

<success_criteria>
All 6 Forge config files exist with correct content. No Claude-specific syntax in any .forge/ file. Existing tests still pass.
</success_criteria>

<output>
After completion, create `.planning/phases/01-spec-core-skill-and-forge-config/01-03-SUMMARY.md`
</output>
