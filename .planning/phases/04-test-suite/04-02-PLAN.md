---
phase: 04-test-suite
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - tests/test_fallback_ladder.bash
  - tests/run_all.bash
autonomous: true
requirements: [TEST-04, TEST-05]

must_haves:
  truths:
    - "test_fallback_ladder.bash passes with PASS count >= 4 and FAIL = 0"
    - "run_all.bash includes all 8 suites and exits 0"
    - "Existing 43 tests still pass"
  artifacts:
    - path: "tests/test_fallback_ladder.bash"
      provides: "Fallback ladder structure tests"
    - path: "tests/run_all.bash"
      provides: "Updated test runner with all 8 suites"
  key_links:
    - from: "tests/run_all.bash"
      to: "tests/test_fallback_ladder.bash"
      via: "run_suite call"
      pattern: "run_suite.*fallback"
---

<objective>
Create the fallback ladder test suite (TEST-04) and integrate all new suites into run_all.bash (TEST-05).

Purpose: Complete test coverage and ensure all suites run together without breaking existing tests.
Output: test_fallback_ladder.bash + updated run_all.bash.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-test-suite/04-CONTEXT.md
@tests/test_install_sh.bash
@tests/run_all.bash
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test_fallback_ladder.bash (TEST-04)</name>
  <files>tests/test_fallback_ladder.bash</files>
  <read_first>tests/test_install_sh.bash, skills/forge/SKILL.md</read_first>
  <action>
Create tests/test_fallback_ladder.bash following test_install_sh.bash pattern. Tests (per D-03):
- T1: Level 1 section contains "Guide" (grep -i "guide" in Level 1 area)
- T2: Level 2 section contains "Handhold" and mentions subtask decomposition or "decompos" or "subtask"
- T3: Level 2 mentions 3-attempt limit (grep for "3\|three" near retry/attempt context)
- T4: Level 3 section contains "Take over"
- T5: Level 3 section contains DEBRIEF or debrief template reference
- T6: Failure Detection section covers signal types (grep for "error\|stall\|wrong output" or similar detection signals)

Use SKILL_FILE="${PLUGIN_DIR}/skills/forge/SKILL.md". Make executable.
  </action>
  <verify>
    <automated>bash tests/test_fallback_ladder.bash</automated>
  </verify>
  <done>test_fallback_ladder.bash runs with 0 failures</done>
  <acceptance_criteria>
    - bash tests/test_fallback_ladder.bash exits 0
    - grep -q 'set -euo pipefail' tests/test_fallback_ladder.bash
    - Output contains "Results:" and "0 failed"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update run_all.bash to include all 4 new suites (TEST-05)</name>
  <files>tests/run_all.bash</files>
  <read_first>tests/run_all.bash</read_first>
  <action>
Append 4 new run_suite calls to tests/run_all.bash AFTER the existing 4 suites (per D-04):

run_suite "Forge skill structure tests"      "test_forge_skill.bash"
run_suite "AGENTS.md deduplication tests"    "test_agents_md_dedup.bash"
run_suite "Skill injection tests"            "test_skill_injection.bash"
run_suite "Fallback ladder tests"            "test_fallback_ladder.bash"

Do NOT modify any existing lines. Insert before the final summary echo block.
  </action>
  <verify>
    <automated>bash tests/run_all.bash</automated>
  </verify>
  <done>run_all.bash runs all 8 suites, exits 0, no suite failures</done>
  <acceptance_criteria>
    - grep -c 'run_suite' tests/run_all.bash returns 8
    - bash tests/run_all.bash exits 0
    - Output contains "ALL SUITES PASSED"
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test scripts → filesystem | Tests read project files; no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | T (Tampering) | run_all.bash | accept | Developer-run locally; no elevated privileges |
</threat_model>

<verification>
bash tests/run_all.bash exits 0 with all 8 suites passing.
</verification>

<success_criteria>
- All 8 suites pass (4 existing + 4 new)
- Output shows "ALL SUITES PASSED"
- No existing test regresses
</success_criteria>

<output>
After completion, create `.planning/phases/04-test-suite/04-02-SUMMARY.md`
</output>
