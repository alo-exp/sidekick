---
phase: 04-test-suite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_forge_skill.bash
  - tests/test_agents_md_dedup.bash
  - tests/test_skill_injection.bash
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03]

must_haves:
  truths:
    - "test_forge_skill.bash passes with PASS count >= 7 and FAIL = 0"
    - "test_agents_md_dedup.bash passes with PASS count >= 3 and FAIL = 0"
    - "test_skill_injection.bash passes with PASS count >= 4 and FAIL = 0"
  artifacts:
    - path: "tests/test_forge_skill.bash"
      provides: "Activation/deactivation/fallback section tests"
    - path: "tests/test_agents_md_dedup.bash"
      provides: "AGENTS.md dedup and session log tests"
    - path: "tests/test_skill_injection.bash"
      provides: "Skill injection infrastructure tests"
  key_links:
    - from: "tests/test_forge_skill.bash"
      to: "skills/forge/SKILL.md"
      via: "grep assertions"
      pattern: "grep.*skills/forge/SKILL.md"
---

<objective>
Create three test suites covering skill activation/deactivation (TEST-01), AGENTS.md deduplication (TEST-02), and skill injection (TEST-03).

Purpose: Provide automated regression coverage for Phases 1-3 behavior.
Output: Three executable bash test files in tests/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-test-suite/04-CONTEXT.md
@tests/test_install_sh.bash
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test_forge_skill.bash (TEST-01)</name>
  <files>tests/test_forge_skill.bash</files>
  <read_first>tests/test_install_sh.bash, skills/forge/SKILL.md</read_first>
  <action>
Create tests/test_forge_skill.bash following the exact pattern of test_install_sh.bash (shebang, set -euo pipefail, PASS/FAIL/SKIP counters, assert_pass/assert_fail/skip helpers, results summary, exit 1 on failure).

Tests to implement (per D-03):
- T1: SKILL.md YAML frontmatter contains "name: forge-delegation"
- T2: Activation section present (grep for "## Activation")
- T3: Health Check subsection present (grep for "Health Check")
- T4: Deactivation section present (grep for "Deactivat")
- T5: Level 1 fallback section present (grep for "Level 1.*Guide" or "Guide")
- T6: Level 2 fallback section present (grep for "Level 2.*Handhold" or "Handhold")
- T7: Level 3 fallback section present (grep for "Level 3.*Take over" or "Take over")
- T8: AGENTS.md Mentoring section present (grep for "AGENTS.md" and "Mentoring")
- T9: Token Optimization section present (grep for "Token" and "Optim")
- T10: Skill Injection section present (grep for "Skill Injection" or "Injection")

Use SKILL_FILE="${PLUGIN_DIR}/skills/forge/SKILL.md" with PLUGIN_DIR derived same as test_install_sh.bash.

Make file executable (chmod +x).
  </action>
  <verify>
    <automated>bash tests/test_forge_skill.bash</automated>
  </verify>
  <done>test_forge_skill.bash runs, all tests PASS, exit 0</done>
  <acceptance_criteria>
    - grep -q 'set -euo pipefail' tests/test_forge_skill.bash
    - grep -q 'assert_pass\|assert_fail' tests/test_forge_skill.bash
    - bash tests/test_forge_skill.bash exits 0
    - Output contains "Results:" and "passed" and "0 failed"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create test_agents_md_dedup.bash (TEST-02) and test_skill_injection.bash (TEST-03)</name>
  <files>tests/test_agents_md_dedup.bash, tests/test_skill_injection.bash</files>
  <read_first>tests/test_install_sh.bash, AGENTS.md, .forge/skills/quality-gates/SKILL.md</read_first>
  <action>
Create tests/test_agents_md_dedup.bash following test_install_sh.bash pattern. Tests (per D-03):
- T1: AGENTS.md exists at project root
- T2: AGENTS.md contains "## Project Conventions" section
- T3: skills/forge/SKILL.md mentions deduplication (grep for "dedup\|duplicate\|semantically equivalent")
- T4: docs/sessions/ directory exists

Create tests/test_skill_injection.bash following test_install_sh.bash pattern. Tests (per D-03):
- T1: .forge/skills/quality-gates/SKILL.md exists
- T2: .forge/skills/code-review/SKILL.md exists
- T3: .forge/skills/security/SKILL.md exists
- T4: .forge/skills/testing-strategy/SKILL.md exists
- T5: Each bootstrap skill has YAML frontmatter with "trigger" field (grep -l 'trigger:' on each)
- T6: No bootstrap skill contains Claude-specific tool names (grep -rL 'Read\|Write\|Edit\|Bash\|AskUserQuestion\|TodoWrite' .forge/skills/ — but use grep -c to verify count is 0 for those patterns as literal tool invocations like "Use the Read tool")
- T7: skills/forge/SKILL.md contains skill mapping table (grep for "mapping\|inject")

Make both files executable.
  </action>
  <verify>
    <automated>bash tests/test_agents_md_dedup.bash && bash tests/test_skill_injection.bash</automated>
  </verify>
  <done>Both suites run with 0 failures</done>
  <acceptance_criteria>
    - bash tests/test_agents_md_dedup.bash exits 0
    - bash tests/test_skill_injection.bash exits 0
    - grep -q 'set -euo pipefail' tests/test_agents_md_dedup.bash
    - grep -q 'set -euo pipefail' tests/test_skill_injection.bash
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test scripts → filesystem | Tests read project files; no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | T (Tampering) | test scripts | accept | Tests are developer-run locally; no elevated privileges |
</threat_model>

<verification>
All three test files exist, are executable, and pass with 0 failures.
</verification>

<success_criteria>
- bash tests/test_forge_skill.bash → 0 failures
- bash tests/test_agents_md_dedup.bash → 0 failures
- bash tests/test_skill_injection.bash → 0 failures
</success_criteria>

<output>
After completion, create `.planning/phases/04-test-suite/04-01-SUMMARY.md`
</output>
