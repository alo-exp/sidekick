---
phase: 02-fallback-ladder-and-skill-injection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/forge/SKILL.md
  - .claude-plugin/plugin.json
autonomous: true
requirements:
  - FALL-01
  - FALL-02
  - FALL-03
  - FALL-04
  - FALL-05

must_haves:
  truths:
    - "On Forge failure, Claude automatically reframes and retries (Level 1)"
    - "If Level 1 fails, Claude decomposes into subtasks and submits sequentially (Level 2)"
    - "If Level 2 fails, Claude acts directly and produces a debrief (Level 3)"
    - "Failure detection covers error signals, wrong output, and stall conditions"
    - "DLGT-04 is lifted only during Level 3 and restored after"
  artifacts:
    - path: "skills/forge/SKILL.md"
      provides: "Fallback ladder sections appended"
      contains: "## Fallback Ladder"
    - path: ".claude-plugin/plugin.json"
      provides: "Updated integrity hash"
  key_links:
    - from: "skills/forge/SKILL.md"
      to: "skills/forge.md STEP 5"
      via: "reference to existing failure patterns"
      pattern: "STEP 5"
---

<objective>
Implement the three-level fallback ladder (Guide, Handhold, Take over) in skills/forge/SKILL.md.

Purpose: When Forge fails a task, Claude needs explicit instructions for escalation behavior — from gentle retry to full takeover — without user intervention.
Output: Extended SKILL.md with fallback ladder sections, updated plugin hash.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/forge-delegation-spec.md
@.planning/phases/02-fallback-ladder-and-skill-injection/02-CONTEXT.md
@skills/forge/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append Failure Detection and Fallback Ladder sections to skills/forge/SKILL.md</name>
  <files>skills/forge/SKILL.md</files>
  <read_first>
    - skills/forge/SKILL.md
    - .planning/forge-delegation-spec.md (sections 6 and 7)
    - .planning/phases/02-fallback-ladder-and-skill-injection/02-CONTEXT.md (decisions D-02 through D-05, D-09)
  </read_first>
  <action>
Append the following sections AFTER the existing "## Deactivation" section in skills/forge/SKILL.md. Do NOT modify any existing content above.

**Section to append: `## Failure Detection`**

After each Forge output, Claude runs three checks:

1. **Error signal check:** Forge output contains "Error:", "Failed:", "fatal:", or exit code != 0. If detected -> trigger Level 1.
2. **Wrong output check:** Forge output does not satisfy SUCCESS CRITERIA from the task prompt. If the SAME failure mode appears on retry -> failure confirmed, trigger next level.
3. **Stall check:** Forge asks a clarifying question back without making progress (behavioral stall in interactive ZSH). Treat as Level 1 trigger — reframe with more specifics.

Reference: `skills/forge.md` STEP 5 for contextual failure recovery patterns.

**Section to append: `## Fallback Ladder`**

Preamble: When failure is detected, escalate sequentially. No level may be skipped.

### Level 1 -- Guide

On failure detection, Claude rewrites the task prompt with:
1. A diagnosis of what Forge likely misunderstood
2. A tighter DESIRED STATE description
3. A concrete code snippet or file diff as reference

Single retry at this level. If retry fails -> escalate to Level 2.

### Level 2 -- Handhold

Claude decomposes the original task into atomic subtasks (each <= 200 tokens). Each subtask gets its own full 5-field prompt (OBJECTIVE, CONTEXT, DESIRED STATE, SUCCESS CRITERIA, INJECTED SKILLS). Submit sequentially, verify output of each before proceeding.

Maximum 3 subtask attempts total. If all 3 fail -> escalate to Level 3.

### Level 3 -- Take over

DLGT-04 restriction is temporarily lifted. Claude uses Write/Edit/Bash tools directly to complete the task. After completion, produce a structured debrief:

```
DEBRIEF:
  TASK: [what the task was]
  FORGE_FAILURE: [why Forge failed — specific diagnosis]
  LEARNED: [what was discovered about Forge's limitations or task characteristics]
  AGENTS_UPDATE: [exact text proposed for ./AGENTS.md to prevent recurrence]
```

The AGENTS_UPDATE is a proposed addition in Forge AGENTS.md format (action-oriented, specific). Claude asks the user to confirm before writing to AGENTS.md.

After Level 3 completes, DLGT-04 is restored — the marker file remains active.
  </action>
  <verify>
    <automated>grep -c "## Failure Detection" skills/forge/SKILL.md && grep -c "## Fallback Ladder" skills/forge/SKILL.md && grep -c "Level 1 -- Guide" skills/forge/SKILL.md && grep -c "Level 2 -- Handhold" skills/forge/SKILL.md && grep -c "Level 3 -- Take over" skills/forge/SKILL.md && grep -c "DEBRIEF:" skills/forge/SKILL.md && grep -c "DLGT-04" skills/forge/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "## Failure Detection" skills/forge/SKILL.md returns exactly 1 match
    - grep "## Fallback Ladder" skills/forge/SKILL.md returns exactly 1 match
    - grep "Level 1 -- Guide" skills/forge/SKILL.md returns 1 match
    - grep "Level 2 -- Handhold" skills/forge/SKILL.md returns 1 match
    - grep "Level 3 -- Take over" skills/forge/SKILL.md returns 1 match
    - grep "DEBRIEF:" skills/forge/SKILL.md returns at least 1 match
    - grep "Error signal check" skills/forge/SKILL.md returns 1 match
    - grep "Wrong output check" skills/forge/SKILL.md returns 1 match
    - grep "Stall check" skills/forge/SKILL.md returns 1 match
    - grep "DLGT-04 restriction is temporarily lifted" skills/forge/SKILL.md returns 1 match
    - The original 67 lines (health check, activation, delegation, deactivation) are unchanged
    - grep "STEP 5" skills/forge/SKILL.md confirms reference to existing forge.md patterns
  </acceptance_criteria>
  <done>skills/forge/SKILL.md contains Failure Detection and Fallback Ladder sections with all three levels, debrief template, and DLGT-04 enforcement rules</done>
</task>

<task type="auto">
  <name>Task 2: Update plugin.json integrity hash</name>
  <files>.claude-plugin/plugin.json</files>
  <read_first>
    - .claude-plugin/plugin.json
    - skills/forge/SKILL.md
  </read_first>
  <action>
Compute the SHA-256 hash of the updated `skills/forge/SKILL.md` file using:
```
shasum -a 256 skills/forge/SKILL.md
```
Then update the hash value in `.claude-plugin/plugin.json` for the forge-delegation skill entry. The field is typically named "integrity" or "sha256" — read the file first to confirm the exact field name, then update the value.
  </action>
  <verify>
    <automated>EXPECTED=$(shasum -a 256 skills/forge/SKILL.md | awk '{print $1}') && grep -q "$EXPECTED" .claude-plugin/plugin.json && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - The SHA-256 hash in .claude-plugin/plugin.json matches the actual hash of skills/forge/SKILL.md
    - The verify command prints "PASS"
  </acceptance_criteria>
  <done>plugin.json integrity hash matches the updated SKILL.md content</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude -> Forge | Claude sends prompts to Forge; Forge output is untrusted until validated against SUCCESS CRITERIA |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Elevation of Privilege | Level 3 fallback | mitigate | DLGT-04 lifted only after Level 1+2 exhausted; restored immediately after; user confirms AGENTS.md writes |
| T-02-02 | Tampering | AGENTS_UPDATE field | mitigate | Claude proposes update text but requires user confirmation before writing to AGENTS.md |
</threat_model>

<verification>
- All 43 existing tests pass: `npm test` or equivalent test runner
- skills/forge/SKILL.md contains both new sections appended after Deactivation
- plugin.json hash matches actual file hash
</verification>

<success_criteria>
- Fallback ladder fully documented in SKILL.md with three levels, failure detection, and debrief template
- Plugin integrity hash updated
- Existing tests unbroken
</success_criteria>

<output>
After completion, create `.planning/phases/02-fallback-ladder-and-skill-injection/02-01-SUMMARY.md`
</output>
