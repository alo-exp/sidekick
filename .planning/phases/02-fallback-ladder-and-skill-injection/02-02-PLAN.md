---
phase: 02-fallback-ladder-and-skill-injection
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - skills/forge/SKILL.md
  - .claude-plugin/plugin.json
autonomous: true
requirements:
  - SINJ-01
  - SINJ-02
  - SINJ-03
  - SINJ-04
  - SINJ-05

must_haves:
  truths:
    - "Claude maintains a mapping of task types to Forge skills"
    - "Before each delegation, Claude identifies relevant skills and lists them in INJECTED SKILLS field"
    - "Only skills matching the task type are injected — not all skills"
    - "Forge auto-detects injected skills via its Skill Engine trigger keywords"
    - "Injected skill files use Forge-compatible format (no Claude-specific syntax)"
  artifacts:
    - path: "skills/forge/SKILL.md"
      provides: "Skill injection section appended"
      contains: "## Skill Injection"
    - path: ".claude-plugin/plugin.json"
      provides: "Updated integrity hash"
  key_links:
    - from: "skills/forge/SKILL.md"
      to: ".forge/skills/"
      via: "mapping table references bootstrap skill paths"
      pattern: ".forge/skills/"
---

<objective>
Implement the selective skill injection layer in skills/forge/SKILL.md.

Purpose: Before each delegation, Claude must decide which skills are relevant and include them in the task prompt's INJECTED SKILLS field so Forge's Skill Engine activates only what's needed.
Output: Extended SKILL.md with skill injection section, updated plugin hash.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/forge-delegation-spec.md
@.planning/phases/02-fallback-ladder-and-skill-injection/02-CONTEXT.md
@skills/forge/SKILL.md
@.planning/phases/02-fallback-ladder-and-skill-injection/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append Skill Injection section to skills/forge/SKILL.md</name>
  <files>skills/forge/SKILL.md</files>
  <read_first>
    - skills/forge/SKILL.md (current state after Plan 01)
    - .planning/forge-delegation-spec.md (section 8)
    - .planning/phases/02-fallback-ladder-and-skill-injection/02-CONTEXT.md (decisions D-06, D-07, D-08)
    - .forge/skills/quality-gates/SKILL.md
    - .forge/skills/security/SKILL.md
    - .forge/skills/testing-strategy/SKILL.md
    - .forge/skills/code-review/SKILL.md
  </read_first>
  <action>
Append the following section AFTER the "## Fallback Ladder" section (added in Plan 01) in skills/forge/SKILL.md. Do NOT modify any existing content above.

**Section to append: `## Skill Injection`**

Before each delegation, Claude performs a skill injection step:

### 1. Determine Task Type

Read the task and classify:
- **Testing task:** task involves writing or running tests
- **Code change:** task involves editing or writing files
- **Security-sensitive:** task involves auth, input validation, credentials, or data handling
- **Review:** task is a general code review
- **Research/read-only:** pure file read or research (no injection needed)

Multiple classifications can apply simultaneously.

### 2. Apply Mapping Table

| Task Type | Injected Skills |
|-----------|----------------|
| Testing | `testing-strategy` |
| Code change | `quality-gates`, `code-review` |
| Security-sensitive | `security` |
| Review | `code-review` |
| Research/read-only | (none) |

### 3. Verify Skill Files Exist

For each skill to inject, confirm the file exists at `.forge/skills/<name>/SKILL.md`. These were bootstrapped in Phase 1 activation and should already be present. If missing, log a warning but proceed with delegation.

### 4. Include in Task Prompt

Add the INJECTED SKILLS field to the task prompt listing only the matched skill names:

```
INJECTED SKILLS: quality-gates, code-review
```

Forge's Skill Engine auto-detects skills via `trigger` keywords in their YAML frontmatter. The INJECTED SKILLS field serves as an explicit signal reinforcing which skills apply.

### Skill Format Requirements

All files in `.forge/skills/` MUST conform to Forge-compatible SKILL.md format:
- YAML frontmatter with: `id`, `title`, `description`, `trigger`
- No `Skill tool` references
- No `AskUserQuestion` calls
- No Claude-specific tool names (use generic: "write file", "run command")
- Imperative language throughout

### Scope Limit

Only the 4 bootstrap skills are in scope: `quality-gates`, `security`, `testing-strategy`, `code-review`. Adding new skills requires a future phase.
  </action>
  <verify>
    <automated>grep -c "## Skill Injection" skills/forge/SKILL.md && grep -c "INJECTED SKILLS" skills/forge/SKILL.md && grep -c "quality-gates" skills/forge/SKILL.md && grep -c "testing-strategy" skills/forge/SKILL.md && grep -c "security" skills/forge/SKILL.md && grep -c "code-review" skills/forge/SKILL.md && grep -c "Skill Format Requirements" skills/forge/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "## Skill Injection" skills/forge/SKILL.md returns exactly 1 match
    - grep "INJECTED SKILLS" skills/forge/SKILL.md returns at least 1 match
    - The mapping table contains all 4 task types with correct skill assignments
    - grep "Forge-compatible SKILL.md format" or "Skill Format Requirements" returns 1 match
    - grep ".forge/skills/" skills/forge/SKILL.md returns at least 1 match
    - grep "Scope Limit" skills/forge/SKILL.md returns 1 match
    - No Claude-specific syntax mentioned as valid in the injection section
    - Original content (activation, delegation, deactivation, fallback ladder) is unchanged
  </acceptance_criteria>
  <done>skills/forge/SKILL.md contains Skill Injection section with mapping table, format requirements, and scope limit</done>
</task>

<task type="auto">
  <name>Task 2: Update plugin.json integrity hash</name>
  <files>.claude-plugin/plugin.json</files>
  <read_first>
    - .claude-plugin/plugin.json
    - skills/forge/SKILL.md
  </read_first>
  <action>
Compute the SHA-256 hash of the final `skills/forge/SKILL.md` file using:
```
shasum -a 256 skills/forge/SKILL.md
```
Then update the hash value in `.claude-plugin/plugin.json` for the forge-delegation skill entry.
  </action>
  <verify>
    <automated>EXPECTED=$(shasum -a 256 skills/forge/SKILL.md | awk '{print $1}') && grep -q "$EXPECTED" .claude-plugin/plugin.json && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - The SHA-256 hash in .claude-plugin/plugin.json matches the actual hash of skills/forge/SKILL.md
    - The verify command prints "PASS"
  </acceptance_criteria>
  <done>plugin.json integrity hash matches the final SKILL.md content</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude -> .forge/skills/ | Skill files on disk could be tampered; Claude verifies existence but trusts content from Phase 1 bootstrap |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Information Disclosure | Skill injection | accept | Skills contain only coding guidelines, no secrets; low risk |
| T-02-04 | Tampering | .forge/skills/ files | accept | Files created by Phase 1 bootstrap; integrity assumed from controlled environment |
</threat_model>

<verification>
- All 43 existing tests pass
- skills/forge/SKILL.md contains Skill Injection section after Fallback Ladder
- plugin.json hash matches actual file hash
- The 4 bootstrap skill files in .forge/skills/ are NOT modified (only referenced)
</verification>

<success_criteria>
- Skill injection protocol fully documented in SKILL.md with mapping table, format rules, and scope limit
- Plugin integrity hash updated
- Existing tests unbroken
- No modifications to .forge/skills/ content (they already exist from Phase 1)
</success_criteria>

<output>
After completion, create `.planning/phases/02-fallback-ladder-and-skill-injection/02-02-SUMMARY.md`
</output>
