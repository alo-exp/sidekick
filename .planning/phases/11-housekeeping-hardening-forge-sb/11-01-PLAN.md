---
phase: 11-housekeeping-hardening-forge-sb
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hooks/forge-progress-surface.sh
autonomous: true
requirements:
  - STRIP-01
  - RDRCT-01

must_haves:
  truths:
    - "strip_ansi uses -0777 -pe (slurp mode) so multi-line OSC sequences are fully consumed in one pass"
    - "The sk- redaction regex covers base64, dot-delimited, and slash-delimited key formats with a 10-char minimum suffix"
    - "Word-boundary \\b is replaced by a lookahead so tokens at end-of-line and before punctuation are caught"
  artifacts:
    - path: "hooks/forge-progress-surface.sh"
      provides: "Updated strip_ansi and sk- redaction in PostToolUse hook"
      contains: "perl -0777 -pe"
  key_links:
    - from: "strip_ansi()"
      to: "extract_status_block()"
      via: "pipe in main()"
      pattern: "strip_ansi.*extract_status_block"
---

<objective>
Fix two credential-safety bugs in hooks/forge-progress-surface.sh:

1. STRIP-01 — switch strip_ansi from line-by-line perl (-pe) to slurp mode (-0777 -pe) so OSC sequences whose BEL/ST terminator lands on the next line are fully consumed rather than leaking their body text into additionalContext.

2. RDRCT-01 — extend the sk- redaction regex: broaden char class to [A-Za-z0-9_\-\.\/+], reduce minimum matched suffix to 10 chars, replace \b word-boundary with a lookahead (?=\s|['">,}]|$) that fires at end-of-line and before JSON/YAML punctuation.

Purpose: prevent secret leakage via multi-line OSC bodies and broaden coverage of sk- style API keys (OpenAI, Anthropic, custom) that use dots, slashes, or plus signs in their token suffix.
Output: single file change to hooks/forge-progress-surface.sh; plugin.json integrity hash for this file must be refreshed in Wave 4 (plan 04).
</objective>

<execution_context>
@/Users/shafqat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shafqat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Current strip_ansi implementation (hooks/forge-progress-surface.sh lines 45-59) -->
```bash
strip_ansi() {
  perl -pe '
    s/\x1b\[[0-9;?]*[ -\/]*[@-~]//g;
    s/\x1b\][^\x07\x1b]*(\x07|\x1b\\)//g;
    s/\x1b[@-Z\\-_]//g;
    s/[\x00-\x08\x0b-\x1f\x7f]//g;
  '
}
```

<!-- Current sk- redaction line (hooks/forge-progress-surface.sh line 133) -->
```bash
s/\bsk-[A-Za-z0-9_-]{16,}\b/[REDACTED-SK-TOKEN]/g;
```

<!-- Usage in main(): strip then extract -->
```bash
status_block="$(printf '%s' "$output" | strip_ansi | extract_status_block || true)"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Switch strip_ansi to slurp mode and extend sk- redaction regex</name>
  <files>hooks/forge-progress-surface.sh</files>
  <behavior>
    - strip_ansi: perl -0777 -pe processes the entire input as a single string so OSC \x1b]...\x07 sequences spanning two lines are caught by the existing regex
    - sk- regex: character class changed from [A-Za-z0-9_-] to [A-Za-z0-9_\-\.\/+]; minimum count changed from {16,} to {10,}; \b word boundaries replaced with (?=\s|['">,}]|$) lookahead on the trailing side; leading \b removed (sk- prefix already anchors the start)
    - All other redaction rules (Authorization:, api_key, ghp_, github_pat_, xox*) remain unchanged
    - Source-guard at bottom of file preserved unchanged
  </behavior>
  <action>
Edit hooks/forge-progress-surface.sh with exactly two targeted changes:

1. In strip_ansi(), change `perl -pe '` to `perl -0777 -pe '` (insert `-0777` flag before `-pe`). The four substitution lines inside the function body are unchanged.

2. In the secret redaction block inside main(), find the sk- line:
   ```
   s/\bsk-[A-Za-z0-9_-]{16,}\b/[REDACTED-SK-TOKEN]/g;
   ```
   Replace with:
   ```
   s/sk-[A-Za-z0-9_\-\.\/+]{10,}(?=\s|['">,}]|$)/[REDACTED-SK-TOKEN]/g;
   ```
   Note: the Perl regex uses a lookahead rather than \b; both leading and trailing \b are removed because the broader char class and explicit lookahead provide cleaner boundaries without false-negatives on lines ending with the token.

No other lines in the file change.
  </action>
  <verify>
    <automated>
      # 1. Confirm -0777 flag is present
      grep -qF 'perl -0777 -pe' hooks/forge-progress-surface.sh && echo "STRIP-01 OK" || echo "STRIP-01 FAIL"
      # 2. Confirm new sk- pattern present
      grep -qF 'sk-[A-Za-z0-9_\-\.\/+]{10,}' hooks/forge-progress-surface.sh && echo "RDRCT-01 OK" || echo "RDRCT-01 FAIL"
      # 3. Old pattern absent
      grep -qF 'sk-[A-Za-z0-9_-]{16,}' hooks/forge-progress-surface.sh && echo "OLD-PATTERN STILL PRESENT" || echo "OLD-PATTERN GONE OK"
      # 4. bash syntax check
      bash -n hooks/forge-progress-surface.sh && echo "SYNTAX OK"
    </automated>
  </verify>
  <done>
    - hooks/forge-progress-surface.sh passes bash -n (no syntax errors)
    - grep confirms 'perl -0777 -pe' is present
    - grep confirms new sk- regex character class [A-Za-z0-9_\-\.\/+]{10,} is present
    - Old \bsk-[A-Za-z0-9_-]{16,}\b pattern is absent
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Forge stdout -> additionalContext | Forge subprocess output (untrusted) flows through strip_ansi then redaction before reaching Claude's transcript |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Information Disclosure | strip_ansi multi-line OSC bypass | mitigate | Switch to -0777 slurp mode (STRIP-01) — OSC sequences spanning two lines are now consumed in one pass |
| T-11-02 | Information Disclosure | sk- token variants (dots, slashes, base64) escaping redaction | mitigate | Broaden char class and reduce min length (RDRCT-01) — catches OpenAI, Anthropic, and custom sk- formats |
| T-11-03 | Tampering | Crafted OSC body resembling instruction text landing in additionalContext | mitigate | Slurp mode eliminates split-sequence escape; combined with existing 2 MiB stdin cap |
</threat_model>

<verification>
After task completes:
1. `bash -n hooks/forge-progress-surface.sh` exits 0
2. `grep -c 'perl -0777' hooks/forge-progress-surface.sh` returns 1
3. `grep -c 'sk-\[A-Za-z0-9' hooks/forge-progress-surface.sh` returns 1 and shows the new char class
4. Existing test suite still passes: `bash tests/test_forge_progress_surface.bash`
</verification>

<success_criteria>
- strip_ansi function uses -0777 -pe (slurp mode) — STRIP-01 satisfied
- sk- redaction regex uses [A-Za-z0-9_\-\.\/+]{10,} with lookahead — RDRCT-01 satisfied
- No other lines in forge-progress-surface.sh changed
- bash -n passes
- Existing progress surface tests remain green
</success_criteria>

<output>
After completion, create `.planning/phases/11-housekeeping-hardening-forge-sb/11-01-SUMMARY.md`
</output>
