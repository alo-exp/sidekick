---
phase: 05-fix-forge-delegation-blocking-bugs-v1-1-2-patch-missing-tool
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-spec-core-skill-and-forge-config/01-03-PLAN.md
  - .forge/agents/forge.md
  - .forge.toml
  - README.md
  - skills/forge.md
autonomous: true
requirements:
  - FCFG-01
  - FCFG-02

must_haves:
  truths:
    - "Every shipped Forge agent template declares tools: [\"*\"] in its frontmatter"
    - "No shipped artifact (README, skill docs, config template, plan docs) references the invalid model ID qwen/qwen3.6-plus"
    - "The already-patched .forge/agents/forge.md in this repo is committed to git so fresh installs inherit the fix"
    - "Plan 01-03 (the spec-of-record for .forge/agents/forge.md) shows the tools: [\"*\"] field in its template so future executions produce correct output"
    - "A Forge agent provisioned from the shipped templates has non-zero tool access and can perform real file writes"
  artifacts:
    - path: ".planning/phases/01-spec-core-skill-and-forge-config/01-03-PLAN.md"
      provides: "Template for .forge/agents/forge.md including tools field and corrected model ID"
      contains: "tools: [\"*\"]"
    - path: ".forge/agents/forge.md"
      provides: "Working Forge agent override with full tool access"
      contains: "tools: [\"*\"]"
    - path: ".forge.toml"
      provides: "Session config with valid OpenRouter model ID"
      contains: "qwen/qwen3-coder-plus"
    - path: "README.md"
      provides: "Providers and Models table with valid model ID"
      contains: "qwen/qwen3-coder-plus"
    - path: "skills/forge.md"
      provides: "Orchestration skill with valid model ID in all 8 occurrences"
      contains: "qwen/qwen3-coder-plus"
  key_links:
    - from: ".forge/agents/forge.md"
      to: "Forge runtime tool allocation"
      via: "tools: [\"*\"] frontmatter field"
      pattern: "^tools: \\[\"\\*\"\\]"
    - from: ".forge.toml"
      to: "OpenRouter model endpoint"
      via: "model_id field"
      pattern: "model_id = \"qwen/qwen3-coder-plus\""
---

<objective>
Patch the two shipped-artifact bugs that cause Forge delegation to silently fail on fresh Sidekick installs:

1. Bug 1 (CRITICAL): Forge agent template missing `tools: ["*"]` in frontmatter — without this, Forge provisions the agent with zero tools and the model emits fake tool-call text that never executes.
2. Bug 2 (BLOCKING): Docs and config reference the nonexistent OpenRouter model ID `qwen/qwen3.6-plus` — when set as the active model, tool schemas are silently omitted from the API call.

Purpose: Restore Forge delegation from silent-failure to working end-to-end for every fresh Sidekick v1.1.2 installer.
Output: One atomic commit touching 5 files. After this plan, `grep -rn "qwen3.6-plus" . --include="*.md" --include="*.toml"` in the shipped-artifact set returns zero hits, and `.forge/agents/forge.md` contains `tools: ["*"]`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-fix-forge-delegation-blocking-bugs-v1-1-2-patch-missing-tool/05-CONTEXT.md

# Scope reference — the canonical agent template block that Plan 01-03 defines
@.planning/phases/01-spec-core-skill-and-forge-config/01-03-PLAN.md

# Current state of the already-locally-patched agent file (modified but uncommitted)
@.forge/agents/forge.md

<interfaces>
<!-- Current committed Plan 01-03 template (lines 89-121 of 01-03-PLAN.md) lists the required
     frontmatter fields for .forge/agents/forge.md as:

         id: forge
         title: Forge (Sidekick-Orchestrated)
         description: Default Forge agent with Sidekick delegation awareness

     It is MISSING tools: ["*"]. The template must be updated to include it. -->

<!-- The already-applied local edit to .forge/agents/forge.md (head -20 shows):

         ---
         id: forge
         title: Forge (Sidekick-Orchestrated)
         description: Default Forge agent with Sidekick delegation awareness
         tools: ["*"]
         ---

     This file is listed as " M .forge/agents/forge.md" in `git status --short`.
     The edit must be committed as part of this plan — do NOT re-patch it. -->

<!-- Model ID occurrences from discovery grep (authoritative — do not substitute with other
     search results). All must become qwen/qwen3-coder-plus:

       README.md:72           | **OpenRouter** (recommended) | **Qwen 3.6 Plus** `qwen/qwen3.6-plus` | ...
       skills/forge.md:219    model_id = "qwen/qwen3.6-plus"
       skills/forge.md:323    forge config set model open_router qwen/qwen3.6-plus
       skills/forge.md:730    forge config set model open_router qwen/qwen3.6-plus
       skills/forge.md:761    model_id = "qwen/qwen3.6-plus"
       skills/forge.md:903    | General coding | OpenRouter | `qwen/qwen3.6-plus` | Best overall, 1M context |
       skills/forge.md:904    | Screenshot/UI analysis | OpenRouter | `qwen/qwen3.6-plus` | Has vision |
       skills/forge.md:913    forge config set model open_router qwen/qwen3.6-plus       # default
       skills/forge.md:936    forge config set model open_router qwen/qwen3.6-plus  # set model
       .forge.toml:11         model_id = "qwen/qwen3.6-plus"
       .planning/phases/01-spec-core-skill-and-forge-config/01-03-PLAN.md:137   model_id = "qwen/qwen3.6-plus"
     -->

<!-- OUT OF SCOPE for this plan (do not modify):
     - context.md (historical context doc; not shipped to users)
     - SENTINEL-audit-forge.md, SENTINEL-audit-forge-r7.md (audit records; historical)
     - docs/internal/pre-release-quality-gate.md (internal doc; pre-release snapshot)
     - .planning/phases/01-.../01-RESEARCH.md (research record; historical)
     These appear in a naive grep but are not shipped artifacts per CONTEXT.md scope. -->

<!-- Human-readable product-name convention:
     - Current text often reads "Qwen 3.6 Plus" as a display name.
     - The correct OpenRouter product name paired with qwen/qwen3-coder-plus is "Qwen3 Coder Plus".
     - Task 2 updates both the model-ID strings AND the adjacent display-name labels on
       the same lines, so documentation stays internally consistent. Lines where the
       display name appears standalone (without the model ID) on unrelated README lines
       are out of scope — Task 2 targets only the line numbers listed above. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patch Bug 1 — add tools: ["*"] to shipped agent templates and commit the already-modified .forge/agents/forge.md</name>
  <files>
    .planning/phases/01-spec-core-skill-and-forge-config/01-03-PLAN.md,
    .forge/agents/forge.md
  </files>
  <action>
STEP A — Update the Plan 01-03 agent-template block so future executions produce a correct file.

Open `.planning/phases/01-spec-core-skill-and-forge-config/01-03-PLAN.md`.

Find this exact text in the <action> block of Task 1 (currently at lines 91-95):

    Create directory `.forge/agents/` and write this file. It must have YAML frontmatter with:
      id: forge
      title: Forge (Sidekick-Orchestrated)
      description: Default Forge agent with Sidekick delegation awareness

Replace it with:

    Create directory `.forge/agents/` and write this file. It must have YAML frontmatter with:
      id: forge
      title: Forge (Sidekick-Orchestrated)
      description: Default Forge agent with Sidekick delegation awareness
      tools: ["*"]

Then find the <acceptance_criteria> block of that same task (lines 145-157) and add one
new line immediately after the existing "Contains YAML frontmatter with `id: forge`" line:

    - Contains `tools: ["*"]` in frontmatter (grants Forge agent full tool access — without this, agent is provisioned with zero tools and silently emits fake tool-call text)

Do NOT change any other part of 01-03-PLAN.md (model_id on line 137 is handled by Task 2).

STEP B — Commit the existing local edit in `.forge/agents/forge.md`.

`git status --short` currently shows ` M .forge/agents/forge.md`. The file already contains the
`tools: ["*"]` line (verified: `grep -n "^tools:" .forge/agents/forge.md` returns
`5:tools: ["*"]`). Do NOT rewrite or re-patch this file. Simply ensure it remains modified
so Task 3's commit includes it.

If for any reason `grep -n "^tools:" .forge/agents/forge.md` returns empty (meaning the local
edit was lost), re-insert the line `tools: ["*"]` into the frontmatter block, placed after
the `description:` line and before the closing `---`. Frontmatter must end up as:

    ---
    id: forge
    title: Forge (Sidekick-Orchestrated)
    description: Default Forge agent with Sidekick delegation awareness
    tools: ["*"]
    ---

STEP C — Check skills/forge/SKILL.md for inline agent templates.

Confirmation only (no edit expected): run
`grep -n "id: forge" skills/forge/SKILL.md`. This should return zero matches — the file
references Plan 01-03 as the source of truth for the agent template and does NOT inline one.
If matches are found, stop and report; it would mean this plan's scope needs expansion.
  </action>
  <verify>
    <automated>grep -q 'tools: \["\*"\]' .planning/phases/01-spec-core-skill-and-forge-config/01-03-PLAN.md && grep -q '^tools: \["\*"\]' .forge/agents/forge.md && test -z "$(grep -n 'id: forge' skills/forge/SKILL.md)" && echo "OK" || echo "FAIL"</automated>
  </verify>
  <done>
    - `.planning/phases/01-spec-core-skill-and-forge-config/01-03-PLAN.md` frontmatter-template block lists `tools: ["*"]` and the acceptance_criteria mentions it.
    - `.forge/agents/forge.md` contains `tools: ["*"]` on its own frontmatter line and is staged/ready for commit.
    - `skills/forge/SKILL.md` contains no inline agent-frontmatter template (confirmed no scope expansion needed).
  </done>
</task>

<task type="auto">
  <name>Task 2: Patch Bug 2 — replace qwen/qwen3.6-plus with qwen/qwen3-coder-plus across all 10 shipped-artifact references</name>
  <files>
    README.md,
    skills/forge.md,
    .forge.toml,
    .planning/phases/01-spec-core-skill-and-forge-config/01-03-PLAN.md
  </files>
  <action>
Perform exact string replacements. Each replacement is a literal substring swap — do not
reflow, reformat, or alter surrounding text except where a display-name update is called
out below.

REPLACEMENT 1 — `.forge.toml` line 11

  Old: `model_id = "qwen/qwen3.6-plus"`
  New: `model_id = "qwen/qwen3-coder-plus"`

REPLACEMENT 2 — `.planning/phases/01-spec-core-skill-and-forge-config/01-03-PLAN.md` line 137

  Old: `model_id = "qwen/qwen3.6-plus"`
  New: `model_id = "qwen/qwen3-coder-plus"`

REPLACEMENT 3 — `README.md` line 72 (ENTIRE LINE replacement, both display name and model ID)

  Old: `| **OpenRouter** (recommended) | **Qwen 3.6 Plus** `qwen/qwen3.6-plus` | Default. 1M context, vision, $0.33/$1.95/MTok |`
  New: `| **OpenRouter** (recommended) | **Qwen3 Coder Plus** `qwen/qwen3-coder-plus` | Default. 1M context, tool-use, $0.33/$1.95/MTok |`

  Notes on this line:
    - Display name updated from "Qwen 3.6 Plus" to "Qwen3 Coder Plus" so the product name
      matches the real OpenRouter product.
    - "vision" is replaced with "tool-use" because qwen3-coder-plus is a coding-focused
      model with verified tool support on OpenRouter (per CONTEXT.md §Bug 2) rather than a
      vision model. Keeping "vision" would misrepresent capabilities.
    - Prices are unchanged; CONTEXT.md does not instruct otherwise and a price verification
      is out of scope for this patch.

  Out of scope on this plan: README.md line 44 ("Qwen 3.6 Plus — best coding model,
  $0.33/$1.95/MTok"). CONTEXT.md lists only line 72 for README. Leave line 44 untouched.

REPLACEMENT 4 — `skills/forge.md` — 8 occurrences

  At each of the 8 line numbers below, replace the literal substring
  `qwen/qwen3.6-plus` with `qwen/qwen3-coder-plus`. Do NOT change anything else on these
  lines (preserve inline backticks, command prefixes, comments, column formatting):

    Line 219:  `model_id = "qwen/qwen3.6-plus"`
               → `model_id = "qwen/qwen3-coder-plus"`
    Line 323:  `forge config set model open_router qwen/qwen3.6-plus`
               → `forge config set model open_router qwen/qwen3-coder-plus`
    Line 730:  `forge config set model open_router qwen/qwen3.6-plus`
               → `forge config set model open_router qwen/qwen3-coder-plus`
    Line 761:  `model_id = "qwen/qwen3.6-plus"`
               → `model_id = "qwen/qwen3-coder-plus"`
    Line 903:  `| General coding | OpenRouter | `qwen/qwen3.6-plus` | Best overall, 1M context |`
               → `| General coding | OpenRouter | `qwen/qwen3-coder-plus` | Best overall, 1M context |`
    Line 904:  `| Screenshot/UI analysis | OpenRouter | `qwen/qwen3.6-plus` | Has vision |`
               → `| Screenshot/UI analysis | OpenRouter | `qwen/qwen3-coder-plus` | Has vision |`
               (Note: "Has vision" is retained because qwen3-coder-plus in fact does not
               have vision; however, this row's semantic accuracy is a separate concern
               flagged for a future patch — do not rewrite it here. The CONTEXT.md scope is
               strictly the model-ID string swap.)
    Line 913:  `forge config set model open_router qwen/qwen3.6-plus       # default`
               → `forge config set model open_router qwen/qwen3-coder-plus       # default`
               (Preserve the exact number of spaces before `# default`.)
    Line 936:  `forge config set model open_router qwen/qwen3.6-plus  # set model`
               → `forge config set model open_router qwen/qwen3-coder-plus  # set model`

POST-REPLACEMENT VERIFICATION

Run the CONTEXT.md validation command exactly:

    grep -rn "qwen3.6-plus" . --include="*.md" --include="*.toml"

Expected result: only the following three HISTORICAL-RECORD hits may remain — each is an
immutable audit/research artifact explicitly out of scope per CONTEXT.md:

  - context.md:21 and context.md:160                         (historical context)
  - SENTINEL-audit-forge.md:1312, SENTINEL-audit-forge-r7.md:540
  - docs/internal/pre-release-quality-gate.md:213            (historical quality gate)
  - .planning/phases/01-.../01-RESEARCH.md:334               (historical research)
  - .planning/phases/05-.../05-CONTEXT.md                    (this phase's own CONTEXT doc
                                                              — describes the bug; keep)

All SHIPPED artifacts (README.md, skills/forge.md, .forge.toml, 01-03-PLAN.md) must show
zero hits. If any shipped artifact still contains `qwen3.6-plus`, fix it before returning.
  </action>
  <verify>
    <automated>! grep -q "qwen3.6-plus" README.md && ! grep -q "qwen3.6-plus" skills/forge.md && ! grep -q "qwen3.6-plus" .forge.toml && ! grep -q "qwen3.6-plus" .planning/phases/01-spec-core-skill-and-forge-config/01-03-PLAN.md && grep -c "qwen/qwen3-coder-plus" skills/forge.md | grep -q "^8$" && grep -q 'model_id = "qwen/qwen3-coder-plus"' .forge.toml && grep -q "qwen/qwen3-coder-plus" README.md && echo "OK" || echo "FAIL"</automated>
  </verify>
  <done>
    - `.forge.toml` line 11 uses `qwen/qwen3-coder-plus`.
    - `.planning/phases/01-spec-core-skill-and-forge-config/01-03-PLAN.md` line 137 uses `qwen/qwen3-coder-plus`.
    - `README.md` line 72 uses `**Qwen3 Coder Plus** `qwen/qwen3-coder-plus`` with `tool-use` replacing `vision`.
    - `skills/forge.md` contains exactly 8 occurrences of `qwen/qwen3-coder-plus` and zero of `qwen3.6-plus`.
    - No shipped artifact contains `qwen3.6-plus`. Only historical/audit/research docs do, matching CONTEXT.md out-of-scope list.
  </done>
</task>

<task type="auto">
  <name>Task 3: Single atomic commit of all Phase 5-01 changes</name>
  <files>
    (commit only — no new files)
  </files>
  <action>
Stage exactly the five files this plan is authorized to modify:

    git add \
      .planning/phases/01-spec-core-skill-and-forge-config/01-03-PLAN.md \
      .forge/agents/forge.md \
      .forge.toml \
      README.md \
      skills/forge.md

Verify the staged set does not include stray files (especially not `.planning/STATE.md`,
`.planning/ROADMAP.md`, or the new phase directory — those belong to orchestrator/release
commits, not this patch):

    git diff --cached --name-only

Expected output (exactly these five paths, any order):

    .forge.toml
    .forge/agents/forge.md
    .planning/phases/01-spec-core-skill-and-forge-config/01-03-PLAN.md
    README.md
    skills/forge.md

Commit with this exact message (heredoc to preserve formatting):

    git commit -m "$(cat <<'EOF'
    fix(forge-delegation): restore tool access and correct model ID (v1.1.2)

    Two compounding shipped-artifact bugs caused /forge delegation to silently
    succeed without doing any real work on fresh Sidekick installs:

    Bug 1 -- Missing tools field in agent frontmatter. Without tools: ["*"],
    Forge provisioned the agent with zero tools; the model emitted XML/markdown
    that looked like tool calls but never executed. STATUS: SUCCESS was reported
    but no files were written. Fixed by adding tools: ["*"] to .forge/agents/forge.md
    and to the Plan 01-03 template so future installations produce a correct file.

    Bug 2 -- Invalid OpenRouter model ID. Docs and config referenced
    qwen/qwen3.6-plus, which does not exist. When set as the active model, the
    API silently omitted tool schemas. Replaced with qwen/qwen3-coder-plus
    (verified on OpenRouter, 1M context, tool support) across README.md,
    skills/forge.md (8 occurrences), .forge.toml, and Plan 01-03.

    Validation:
      grep -rn "qwen3.6-plus" . --include="*.md" --include="*.toml"
        now returns only historical audit/research records (out of scope)
      grep -n "^tools:" .forge/agents/forge.md
        returns: 5:tools: ["*"]
    EOF
    )"

After the commit, run `git status --short` and confirm:
  - The five paths above no longer appear.
  - The phase directory `.planning/phases/05-.../` and `.planning/ROADMAP.md`, `.planning/STATE.md`
    may still appear as untracked/modified — they belong to Plan 05-02 and the orchestrator.
  </action>
  <verify>
    <automated>git log -1 --format=%s | grep -q "fix(forge-delegation): restore tool access and correct model ID" && git diff HEAD~1 HEAD --name-only | sort | diff - <(printf ".forge.toml\n.forge/agents/forge.md\n.planning/phases/01-spec-core-skill-and-forge-config/01-03-PLAN.md\nREADME.md\nskills/forge.md\n" | sort) && echo "OK" || echo "FAIL"</automated>
  </verify>
  <done>
    - Exactly one new commit exists on HEAD with the specified subject line.
    - The commit touches exactly the five authorized files.
    - `git status --short` no longer lists `.forge/agents/forge.md` or any of the four
      other patched files.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shipped agent template → Forge runtime | Frontmatter `tools:` field controls what tools Forge grants the agent. Misconfiguration causes silent failure (the exact bug being patched). |
| Shipped model ID string → OpenRouter API | Invalid IDs cause silent tool-schema omission. Privacy/security impact low; availability impact critical. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Denial of Service | Forge agent tool allocation | mitigate | Task 1 adds `tools: ["*"]` to shipped template so no user can install an agent with zero tools |
| T-05-01-02 | Denial of Service | OpenRouter model resolution | mitigate | Task 2 replaces invalid model ID across all shipped artifacts with verified ID `qwen/qwen3-coder-plus` |
| T-05-01-03 | Elevation of Privilege | `tools: ["*"]` wildcard | accept | Forge agent inherits project scope; wildcard matches the existing design (no finer-grained tool list exists in current Forge). Documented in AGENTS.md pattern section of CONTEXT.md. |
| T-05-01-04 | Tampering | `.forge/agents/forge.md` in repo | accept | Change is tracked via git; reviewers see diff before merge. Plan 01-03's non-destructive rule (D-05) still applies to install-time behavior. |
</threat_model>

<verification>
After Task 3 completes, run all five CONTEXT.md Validation Commands:

1. `grep -rn "qwen3.6-plus" . --include="*.md" --include="*.toml"`
   → Must show ONLY the historical records listed in Task 2's "POST-REPLACEMENT VERIFICATION"
     (context.md, SENTINEL-audit-*.md, docs/internal/pre-release-quality-gate.md,
     01-RESEARCH.md, 05-CONTEXT.md). Zero shipped-artifact hits.

2. `grep -n "^tools:" .forge/agents/forge.md`
   → Must return `5:tools: ["*"]` (or whichever line hosts the tools field).

3. `forge list tool forge`
   → Must show all tools with `[x]` markers. This is a runtime check that requires a Forge
     binary; if unavailable in the execution environment, mark this step as "deferred to
     Success Criterion 4 smoke test" and record the deferral in the plan summary.

4. `forge -p "write hello to /tmp/sidekick-install-smoke.txt" && cat /tmp/sidekick-install-smoke.txt`
   → Must produce a file containing `hello`. Same environment caveat as #3.

Steps 3 and 4 may be deferred to a clean-install smoke test (handled by `/create-release`
invocation after Plan 05-02) if the current environment has a stale Forge session or
cached agent. Record any deferral clearly in the plan summary.
</verification>

<success_criteria>
- All 10 model-ID occurrences in shipped artifacts are updated to `qwen/qwen3-coder-plus`.
- `.forge/agents/forge.md` and Plan 01-03's agent template both declare `tools: ["*"]`.
- A single git commit on HEAD contains all five file changes with the specified subject line.
- No file outside the five authorized paths is touched by this plan.
- CONTEXT.md Success Criteria 1, 2, and 3 are satisfied. Criterion 4 (live smoke test) may be
  deferred to the post-release verification pass; Criteria 5-7 are handled by Plan 05-02.
</success_criteria>

<output>
After completion, create `.planning/phases/05-fix-forge-delegation-blocking-bugs-v1-1-2-patch-missing-tool/05-01-SUMMARY.md` using the summary template. The summary must include:

- The exact commit SHA of the atomic commit.
- Output of `grep -rn "qwen3.6-plus" . --include="*.md" --include="*.toml"` after the fix,
  with each remaining hit annotated as historical/out-of-scope.
- Output of `grep -n "^tools:" .forge/agents/forge.md`.
- Whether the runtime Forge smoke test (validation steps 3-4) was executed in this pass or
  deferred to the post-release verification, and why.
</output>
