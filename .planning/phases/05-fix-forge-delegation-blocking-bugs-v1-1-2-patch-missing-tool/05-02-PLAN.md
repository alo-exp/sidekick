---
phase: 05-fix-forge-delegation-blocking-bugs-v1-1-2-patch-missing-tool
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - README.md
  - CHANGELOG.md
autonomous: true
requirements:
  - FCFG-01
  - FCFG-02

must_haves:
  truths:
    - "README.md's version badge shows v1.1.2 (not v1.1.1)"
    - "CHANGELOG.md has a new '## 1.1.2 — YYYY-MM-DD' section at the top, above the 1.1.1 entry"
    - "The 1.1.2 CHANGELOG entry describes both Bug 1 (missing tools frontmatter) and Bug 2 (invalid model ID) in user-facing language"
    - "The repository is in a state where `/create-release v1.1.2` will succeed — i.e. the release skill's Step 5 README freshness check will find v1.1.2 in README.md"
    - "Release orchestration (git tag + GitHub release) is NOT performed by this plan — it is the user's responsibility via /create-release afterward"
  artifacts:
    - path: "README.md"
      provides: "Sidekicks table with version badge updated to v1.1.2"
      contains: "v1.1.2"
    - path: "CHANGELOG.md"
      provides: "1.1.2 release notes at top"
      contains: "## 1.1.2"
  key_links:
    - from: "README.md version badge"
      to: "/create-release Step 5 freshness check"
      via: "regex match on current version string"
      pattern: "v1\\.1\\.2"
    - from: "CHANGELOG.md 1.1.2 entry"
      to: "GitHub release body (built by /create-release)"
      via: "extracted section content"
      pattern: "## 1\\.1\\.2"
---

<objective>
Prepare the repository for the v1.1.2 release by bumping the README version badge and adding a CHANGELOG entry describing the two bug fixes shipped in Plan 05-01. Leave the repo in a state where `/create-release v1.1.2` (invoked by the user after this phase) completes cleanly — specifically, its Step 5 README freshness check must find v1.1.2.

Purpose: Separate release artifact updates from code fixes so reviewers see a clean two-commit phase (fix + release prep) and the `/create-release` skill has everything it needs.
Output: One atomic commit touching two files (README.md, CHANGELOG.md). No git tag, no GitHub release — those are the user's next step via the `/create-release` skill.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-fix-forge-delegation-blocking-bugs-v1-1-2-patch-missing-tool/05-CONTEXT.md

# Current version strings
@README.md
@CHANGELOG.md

<interfaces>
<!-- Current CHANGELOG.md top (lines 1-5):

         # Changelog

         ## 1.1.1 — 2026-04-17

         - **Fix**: `/forge` activation health check #3 (credentials present) now correctly ...

     The new 1.1.2 section must be inserted between the `# Changelog` heading and the
     `## 1.1.1` section, separated by a blank line on each side. Date format is
     `YYYY-MM-DD`; use today's date (2026-04-17) unless the current system date differs.
     If the system date has advanced past 2026-04-17 at execution time, use the current
     date instead to match CHANGELOG convention. -->

<!-- Current README.md Sidekicks table (line 9):

         | **Forge** | `forge` | [ForgeCode](https://forgecode.dev) — #2 Terminal-Bench 2.0 (81.8%) | ✅ v1.1.1 |

     The literal substring `v1.1.1` is the badge that must become `v1.1.2`. There is only
     one occurrence of `v1.1.1` in README.md — verify before replacing. -->

<!-- /create-release Step 5 ("README freshness check"):
     The release skill scans README.md for the target version string (v1.1.2). If absent,
     the release is blocked. After Plan 05-02, README.md MUST contain the literal string
     `v1.1.2` so that check passes. -->

<!-- SCOPE CLARIFICATION:
     This plan does NOT:
       - Create or push a git tag (/create-release does this).
       - Create a GitHub release (/create-release does this via `gh release create`).
       - Update any other version artifact (there are no package.json, plugin manifests,
         or schema files with embedded version strings in this repo as of Phase 5).
     If the executor discovers an additional version string that should be bumped (e.g. a
     hidden `VERSION` file), stop and report rather than silently expanding scope. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md version badge from v1.1.1 to v1.1.2</name>
  <files>README.md</files>
  <action>
Confirm the current state first:

    grep -n "v1\.1\.1" README.md

Expected: one line, matching line 9 of README.md. If more than one match is returned, stop
and report — the plan assumed a single occurrence.

Perform the exact literal substring replacement in README.md line 9:

    Old:  `| **Forge** | `forge` | [ForgeCode](https://forgecode.dev) — #2 Terminal-Bench 2.0 (81.8%) | ✅ v1.1.1 |`
    New:  `| **Forge** | `forge` | [ForgeCode](https://forgecode.dev) — #2 Terminal-Bench 2.0 (81.8%) | ✅ v1.1.2 |`

Do NOT change anything else in README.md. In particular:
  - Line 44 "Qwen 3.6 Plus — best coding model" is out of scope (documented in Plan 05-01).
  - Line 72 was already updated by Plan 05-01 — it now shows `qwen/qwen3-coder-plus`. Do
    not touch it.

After the edit, run:

    grep -c "v1\.1\.2" README.md
    grep -c "v1\.1\.1" README.md

Expected: first returns 1 (or more if other v1.1.2 mentions exist — acceptable), second
returns 0. If the second returns non-zero, some v1.1.1 reference remains — investigate.
  </action>
  <verify>
    <automated>grep -q "v1\.1\.2" README.md && ! grep -q "v1\.1\.1" README.md && echo "OK" || echo "FAIL"</automated>
  </verify>
  <done>
    - README.md contains the literal string `v1.1.2` on the Sidekicks table row.
    - README.md contains zero occurrences of `v1.1.1`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add 1.1.2 CHANGELOG entry describing both bug fixes</name>
  <files>CHANGELOG.md</files>
  <action>
Determine the release date. Default to `2026-04-17`. If the current system date reported
by `date +%Y-%m-%d` is later, use that instead — the CHANGELOG convention is date-of-entry.

Open CHANGELOG.md. Find the current top of the file (lines 1-5):

    # Changelog

    ## 1.1.1 — 2026-04-17

    - **Fix**: `/forge` activation health check #3 (credentials present) now correctly validates Forge's current credentials schema (`[{id, auth_details}, ...]`). Prior check only matched the legacy flat `{api_key}` schema, causing false-negative activation failures on valid installs. Both schemas now supported; malformed files fail cleanly instead of producing a jq type error.

Insert a new section BETWEEN `# Changelog` and `## 1.1.1`. The resulting top of the file
must look exactly like this (substitute the correct date if different from 2026-04-17):

    # Changelog

    ## 1.1.2 — 2026-04-17

    - **Fix (CRITICAL)**: Forge agent template was missing the `tools: ["*"]` frontmatter field. Without it, Forge provisioned the agent with zero tools and any model — no matter how capable — emitted XML/markdown text that looked like tool calls but never executed. `/forge` delegation reported `STATUS: SUCCESS` while no files were actually created. Fixed in `.forge/agents/forge.md` and in the Plan 01-03 template so fresh installs inherit the correct configuration.
    - **Fix (BLOCKING)**: Replaced the invalid OpenRouter model ID `qwen/qwen3.6-plus` (which does not exist) with the verified `qwen/qwen3-coder-plus` across README, `skills/forge.md` (8 references), `.forge.toml`, and internal planning artifacts. With the invalid ID set as the active model, the API silently omitted tool schemas, which compounded the Bug 1 symptom. After the fix, `grep -rn "qwen3.6-plus" .` returns only historical audit records.
    - **Docs**: README Providers and Models table now shows `Qwen3 Coder Plus` (`qwen/qwen3-coder-plus`) as the recommended default; capability descriptor updated from "vision" to "tool-use" to match the model's actual feature set.

    ## 1.1.1 — 2026-04-17

    - **Fix**: `/forge` activation health check #3 (credentials present) now correctly validates Forge's current credentials schema (`[{id, auth_details}, ...]`). Prior check only matched the legacy flat `{api_key}` schema, causing false-negative activation failures on valid installs. Both schemas now supported; malformed files fail cleanly instead of producing a jq type error.

Preserve exact blank-line spacing: one blank line between `# Changelog` and `## 1.1.2`,
one blank line between the last bullet of 1.1.2 and `## 1.1.1`, one blank line between
`## 1.1.1` and its bullet. Match the file's existing line-ending convention (LF — do not
introduce CRLF).

Do not modify any pre-existing section (1.1.1, 1.1.0, 1.0.0). Only insert the new 1.1.2
block.

After the edit, run:

    head -5 CHANGELOG.md

Expected first five lines:

    # Changelog

    ## 1.1.2 — <date>

    - **Fix (CRITICAL)**: ...
  </action>
  <verify>
    <automated>grep -q "^## 1\.1\.2" CHANGELOG.md && head -3 CHANGELOG.md | grep -q "## 1\.1\.2" && grep -q "tools: \[\"\*\"\]" CHANGELOG.md && grep -q "qwen/qwen3-coder-plus" CHANGELOG.md && echo "OK" || echo "FAIL"</automated>
  </verify>
  <done>
    - CHANGELOG.md has a `## 1.1.2 — <date>` section as the newest entry.
    - Both bug fixes are described in user-facing language.
    - No existing entry (1.1.1, 1.1.0, 1.0.0) is modified.
  </done>
</task>

<task type="auto">
  <name>Task 3: Atomic commit of release-prep changes</name>
  <files>(commit only — no new files)</files>
  <action>
Stage exactly the two files this plan is authorized to modify:

    git add README.md CHANGELOG.md

Verify the staged set:

    git diff --cached --name-only

Expected output (exactly):

    CHANGELOG.md
    README.md

If any additional files appear, `git restore --staged` them before committing. Do NOT
stage `.planning/STATE.md`, `.planning/ROADMAP.md`, or the phase directory — those are
orchestrator-managed.

Commit with this exact message (heredoc to preserve formatting):

    git commit -m "$(cat <<'EOF'
    release: prepare v1.1.2 (README badge + CHANGELOG entry)

    - Bump README Sidekicks table version badge from v1.1.1 to v1.1.2 so the
      /create-release skill's Step 5 freshness check passes.
    - Add CHANGELOG 1.1.2 entry describing the two forge-delegation bug fixes
      shipped in the preceding commit (missing tools frontmatter; invalid
      OpenRouter model ID).

    This commit is a documentation-only release-prep step. Tag creation and
    GitHub release publication are performed by the user via /create-release
    v1.1.2 after this phase completes.
    EOF
    )"

After commit, run `git log --oneline -3` to confirm the commit history for this phase
looks like (top = newest):

    <sha>  release: prepare v1.1.2 (README badge + CHANGELOG entry)
    <sha>  fix(forge-delegation): restore tool access and correct model ID (v1.1.2)
    <sha>  <prior commit, e.g. fed2625 fix(typography): set mono code elements ...>

Do NOT run `git tag` or `gh release create` — that is the user's next step via
`/create-release v1.1.2`.
  </action>
  <verify>
    <automated>git log -1 --format=%s | grep -q "release: prepare v1.1.2" && git diff HEAD~1 HEAD --name-only | sort | diff - <(printf "CHANGELOG.md\nREADME.md\n" | sort) && ! git tag -l | grep -q "^v1\.1\.2$" && echo "OK" || echo "FAIL"</automated>
  </verify>
  <done>
    - Exactly one new commit on HEAD with subject `release: prepare v1.1.2 (README badge + CHANGELOG entry)`.
    - The commit touches exactly README.md and CHANGELOG.md.
    - No git tag `v1.1.2` exists yet (that is created by `/create-release`, not by this plan).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README.md version badge → /create-release freshness check | Release tooling blocks on missing version string. Low threat, high availability impact. |
| CHANGELOG entry → GitHub release body | /create-release extracts the section verbatim for the GitHub release notes. Content authored here is published to users. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Denial of Service | /create-release execution | mitigate | Task 1 ensures README contains the exact `v1.1.2` string the skill searches for. |
| T-05-02-02 | Information Disclosure | CHANGELOG content | accept | Entry describes bugs in plain user-facing terms; no credentials, no private paths, no CVE-level detail that would enable targeted exploitation of other deployments. |
| T-05-02-03 | Tampering | Git tag fabrication | mitigate | This plan explicitly does NOT create the tag; /create-release creates and signs (if configured) the tag from a verified commit. |
</threat_model>

<verification>
After Task 3 completes:

1. `grep -n "v1\.1\.2" README.md` returns at least one line; `grep -n "v1\.1\.1" README.md` returns nothing.
2. `head -10 CHANGELOG.md` shows the 1.1.2 section as the newest entry with both bug fixes described.
3. `git log --oneline -2 --format='%s'` shows, top to bottom:
       `release: prepare v1.1.2 (README badge + CHANGELOG entry)`
       `fix(forge-delegation): restore tool access and correct model ID (v1.1.2)`
4. `git tag -l "v1.1.2"` returns empty (tag not yet created — that is the user's step).
5. `git status --short` shows no pending changes to README.md or CHANGELOG.md.

CONTEXT.md Success Criterion 4 (post-install smoke test) is BEST executed after
`/create-release v1.1.2` ships the release and a user reinstalls via `install.sh` in a
clean environment, because the smoke test's purpose is to verify fresh-install behavior.
The recommended procedure (document in summary):

    cd "$(mktemp -d)"
    curl -fsSL https://raw.githubusercontent.com/alo-exp/sidekick/v1.1.2/install.sh | bash
    # then in a fresh Claude session:
    /forge
    forge -p "write hello to /tmp/sidekick-install-smoke.txt"
    cat /tmp/sidekick-install-smoke.txt   # must print: hello

Record whether this smoke test is executed in the current pass or deferred.
</verification>

<success_criteria>
- README.md contains `v1.1.2` and no `v1.1.1`.
- CHANGELOG.md has a new 1.1.2 section as the top entry describing both bug fixes.
- A single atomic commit on HEAD contains both files with the specified subject line.
- No git tag or GitHub release is created by this plan.
- Repository is ready for the user to invoke `/create-release v1.1.2`.
- CONTEXT.md Success Criteria 5 (version badge) and 6 (CHANGELOG entry) satisfied.
  Criterion 7 (tag + GitHub release) is the user's next step via /create-release.
</success_criteria>

<output>
After completion, create `.planning/phases/05-fix-forge-delegation-blocking-bugs-v1-1-2-patch-missing-tool/05-02-SUMMARY.md` using the summary template. The summary must include:

- The exact commit SHA of the release-prep commit.
- The final `head -12 CHANGELOG.md` output showing the new 1.1.2 section in context.
- The final `grep -n "v1\.1\." README.md` output confirming badge state.
- A reminder that the user's next step is `/create-release v1.1.2` to create the git tag
  and publish the GitHub release.
- A note on whether the post-install smoke test (CONTEXT.md Success Criterion 4) was run
  in this pass or deferred to post-release clean-install verification.
</output>
