---
phase: 03-agents-md-mentoring-and-token-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/forge/SKILL.md
  - AGENTS.md
  - .claude-plugin/plugin.json
autonomous: true
requirements: [AGNT-01, AGNT-02, AGNT-03, AGNT-04, AGNT-05, AGNT-06, AGNT-07, AGNT-08, TOKN-01]

must_haves:
  truths:
    - "After each Forge task, Claude extracts standing instructions and appends to both ~/forge/AGENTS.md and ./AGENTS.md"
    - "Deduplication prevents semantically equivalent content from being re-appended"
    - "A session log entry is written to docs/sessions/ capturing instruction evolution"
    - "On first /forge invocation with empty AGENTS.md, Claude bootstraps from skills/forge.md content"
  artifacts:
    - path: "skills/forge/SKILL.md"
      provides: "AGENTS.md mentoring loop section appended after line 169"
      contains: "AGENTS.md Mentoring Loop"
    - path: "AGENTS.md"
      provides: "Project-level AGENTS.md template with bootstrap content"
      contains: "Project Conventions"
    - path: ".claude-plugin/plugin.json"
      provides: "Updated forge_skill_md_sha256 hash"
      contains: "forge_skill_md_sha256"
  key_links:
    - from: "skills/forge/SKILL.md"
      to: "~/forge/AGENTS.md"
      via: "mentoring loop write protocol"
      pattern: "AGENTS\\.md"
    - from: "skills/forge/SKILL.md"
      to: "docs/sessions/"
      via: "session log write step"
      pattern: "docs/sessions/"
---

<objective>
Implement the AGENTS.md mentoring loop in skills/forge/SKILL.md — extraction protocol, three-tier write (global, project, session log), deduplication algorithm, and bootstrap behavior.

Purpose: After every Forge task, standing instructions flow into the right tier without duplication, enabling Forge to accumulate knowledge over time.
Output: Extended SKILL.md with mentoring section, project AGENTS.md template, updated plugin hash.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-agents-md-mentoring-and-token-optimization/03-CONTEXT.md
@.planning/forge-delegation-spec.md (sections 9 and 10)
@skills/forge/SKILL.md (current 169 lines — EXTEND, do not replace)
@skills/forge.md (reference ONLY for STEP 6 post-delegation review patterns — NEVER modify)

<interfaces>
<!-- From skills/forge/SKILL.md — current structure to extend -->
The file currently ends at line 169 with the "Skill Injection" section (Phase 2).
New content MUST be appended AFTER line 169 as a new section.

<!-- From .claude-plugin/plugin.json — hash field to update -->
"forge_skill_md_sha256": "edf9a2e155109cc26edee7e5350d6f45ecfbaa575f27167a4bc4581dbaa1f61b"
This hash must be recalculated after SKILL.md modification.

<!-- docs/sessions/ directory exists (empty, has .gitkeep) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append AGENTS.md Mentoring Loop section to SKILL.md</name>
  <files>skills/forge/SKILL.md</files>
  <read_first>skills/forge/SKILL.md, .planning/forge-delegation-spec.md, skills/forge.md (lines 400-500 for STEP 6 patterns)</read_first>
  <action>
Append a new section after line 169 of skills/forge/SKILL.md titled "## AGENTS.md Mentoring Loop". Per D-01 through D-06 from 03-CONTEXT.md, include these subsections:

### Post-Task Extraction (per D-03, AGNT-01)
After every Forge task completion (success OR failure), Claude extracts:
1. Corrections — mistakes Forge made that Claude fixed
2. User preferences — expressed during the session
3. Project patterns — conventions Forge discovered
4. Forge behavior observations — what Forge does well/poorly in this codebase

Format each extraction as action-oriented, specific instructions (not observations).

### Deduplication Algorithm (per D-04, AGNT-04, TOKN-01)
Before every AGENTS.md write, run two-phase check:
1. **Primary:** Exact substring match — scan existing AGENTS.md content for the instruction text
2. **Secondary:** Semantic similarity — if no exact match, check if an equivalent instruction exists phrased differently
3. If either check matches: skip the write entirely (no partial append)

### Three-Tier Write Protocol (per D-02, AGNT-02, AGNT-03, AGNT-05)
After extraction and deduplication:
1. **Global tier:** Append to `~/forge/AGENTS.md` — cross-project knowledge (Code Style, Testing, Git Workflow, Forge Behavior categories per D-05/AGNT-06)
2. **Project tier:** Append to `./AGENTS.md` — project-specific conventions (project structure, task patterns, Forge behavior corrections per D-05/AGNT-07)
3. **Session log:** Write to `docs/sessions/YYYY-MM-DD-session.md` — append entry with: task name, extracted instructions, deduplication decisions (skipped items and why)

If `~/forge/AGENTS.md` does not exist, create it with category headers: Code Style, Testing, Git Workflow, Forge Behavior.

### Bootstrap Behavior (per D-06, AGNT-08)
On first `/forge` invocation when `./AGENTS.md` is empty or absent:
1. Read `skills/forge.md` for key conventions
2. Extract: output format expectations (STATUS/FILES_CHANGED/ASSUMPTIONS/PATTERNS_DISCOVERED), delegation principles (Claude=Brain, Forge=Hands), structured response requirements
3. Write these as the initial content of `./AGENTS.md` under "## Project Conventions" and "## Forge Output Format"
4. Do NOT re-bootstrap if `./AGENTS.md` already has content (check file size > 0)

### AGENTS.md Format (per D-05, AGNT-06, AGNT-07)
Global `~/forge/AGENTS.md` format:
```
# Forge Global Instructions

## Code Style
[action-oriented rules]

## Testing
[action-oriented rules]

## Git Workflow
[action-oriented rules]

## Forge Behavior
[action-oriented rules]
```

Project `./AGENTS.md` format:
```
# Project: [name]

## Project Conventions
[project structure, naming, patterns]

## Forge Output Format
[STATUS/FILES_CHANGED/ASSUMPTIONS/PATTERNS_DISCOVERED]

## Task Patterns
[recurring task types and how to handle them]

## Forge Corrections
[specific corrections for this codebase]
```
  </action>
  <verify>
    <automated>grep -c "AGENTS.md Mentoring Loop" skills/forge/SKILL.md | grep -q "1" && grep -c "Deduplication Algorithm" skills/forge/SKILL.md | grep -q "1" && grep -c "Three-Tier Write Protocol" skills/forge/SKILL.md | grep -q "1" && grep -c "Bootstrap Behavior" skills/forge/SKILL.md | grep -q "1" && echo "PASS"</automated>
  </verify>
  <done>skills/forge/SKILL.md contains AGENTS.md Mentoring Loop section with all 5 subsections (Post-Task Extraction, Deduplication Algorithm, Three-Tier Write Protocol, Bootstrap Behavior, AGENTS.md Format)</done>
  <acceptance_criteria>
- grep "AGENTS.md Mentoring Loop" skills/forge/SKILL.md returns 1 match
- grep "Deduplication Algorithm" skills/forge/SKILL.md returns 1 match
- grep "Three-Tier Write Protocol" skills/forge/SKILL.md returns 1 match
- grep "Bootstrap Behavior" skills/forge/SKILL.md returns 1 match
- grep "docs/sessions/" skills/forge/SKILL.md returns at least 1 match
- grep "~/forge/AGENTS.md" skills/forge/SKILL.md returns at least 1 match
- wc -l skills/forge/SKILL.md shows > 169 lines
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create project AGENTS.md template and update plugin hash</name>
  <files>AGENTS.md, .claude-plugin/plugin.json</files>
  <read_first>.claude-plugin/plugin.json, skills/forge.md (lines 25-30 for delegation principles, lines 350-420 for STEP 6 output format)</read_first>
  <action>
1. Create `./AGENTS.md` at project root with bootstrap content per D-06/AGNT-08. Content:

```markdown
# Project: Sidekick

## Project Conventions

- Shell/Bash + Markdown stack — no compiled languages
- `skills/forge.md` is the core orchestration protocol (862 lines) — NEVER modify
- `skills/forge/SKILL.md` is the user-invoked mode switch — extends forge.md
- Tests live in `tests/` and run via `tests/run_all.bash`
- Plugin manifest at `.claude-plugin/plugin.json` — update hashes when skill files change

## Forge Output Format

After every task, Forge must produce structured output:
- STATUS: success | partial | failed
- FILES_CHANGED: list of files created or modified
- ASSUMPTIONS: any assumptions made during execution
- PATTERNS_DISCOVERED: conventions or patterns noticed in the codebase

## Task Patterns

- Implementation tasks: delegate to Forge with 5-field prompt (OBJECTIVE, CONTEXT, DESIRED STATE, SUCCESS CRITERIA, INJECTED SKILLS)
- Testing tasks: inject testing-strategy skill
- Security-sensitive tasks: inject security skill

## Forge Corrections

(Initially empty — populated by mentoring loop after each task)
```

2. Update `.claude-plugin/plugin.json` — recalculate SHA-256 hash of `skills/forge/SKILL.md` after Task 1 modifications:
   ```bash
   shasum -a 256 skills/forge/SKILL.md | awk '{print $1}'
   ```
   Replace the `forge_skill_md_sha256` value in plugin.json with the new hash.
  </action>
  <verify>
    <automated>test -f AGENTS.md && grep -q "Project: Sidekick" AGENTS.md && grep -q "Forge Output Format" AGENTS.md && NEW_HASH=$(shasum -a 256 skills/forge/SKILL.md | awk '{print $1}') && grep -q "$NEW_HASH" .claude-plugin/plugin.json && echo "PASS"</automated>
  </verify>
  <done>./AGENTS.md exists with bootstrap content matching forge.md conventions; plugin.json hash matches current SKILL.md</done>
  <acceptance_criteria>
- test -f AGENTS.md succeeds
- grep "Project: Sidekick" AGENTS.md returns match
- grep "Forge Output Format" AGENTS.md returns match
- grep "STATUS.*FILES_CHANGED" AGENTS.md returns match
- SHA-256 hash in plugin.json matches actual shasum of skills/forge/SKILL.md
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SKILL.md instructions → Claude behavior | Instructions in SKILL.md guide Claude's writes to AGENTS.md files |
| ~/forge/AGENTS.md → Forge context | Global instructions consumed by Forge on every invocation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | ~/forge/AGENTS.md | accept | User-controlled file on their own machine; no elevated risk |
| T-03-02 | Information Disclosure | docs/sessions/ logs | accept | Session logs contain instruction text only, no secrets; gitignored if sensitive |
| T-03-03 | Denial of Service | AGENTS.md bloat | mitigate | Deduplication algorithm prevents unbounded growth; compaction in plan 03-02 |
</threat_model>

<verification>
- All 43 existing tests pass: `bash tests/run_all.bash`
- skills/forge/SKILL.md has AGENTS.md mentoring section with all subsections
- ./AGENTS.md exists with bootstrap content
- plugin.json hash is current
</verification>

<success_criteria>
SKILL.md extended with complete AGENTS.md mentoring loop protocol. Project AGENTS.md bootstrapped. Plugin integrity hash updated.
</success_criteria>

<output>
After completion, create `.planning/phases/03-agents-md-mentoring-and-token-optimization/03-01-SUMMARY.md`
</output>
