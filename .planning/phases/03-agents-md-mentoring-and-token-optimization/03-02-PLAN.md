---
phase: 03-agents-md-mentoring-and-token-optimization
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - skills/forge/SKILL.md
  - .claude-plugin/plugin.json
autonomous: true
requirements: [TOKN-02, TOKN-03, TOKN-04]

must_haves:
  truths:
    - "Task prompts to Forge contain only the 5 mandatory fields and stay within 2,000 token budget"
    - "Skill injection is limited to 2 skills max unless task is clearly multi-domain"
    - ".forge.toml compaction thresholds are documented as tested defaults with rationale"
  artifacts:
    - path: "skills/forge/SKILL.md"
      provides: "Token optimization section appended after mentoring loop"
      contains: "Token Optimization"
    - path: ".claude-plugin/plugin.json"
      provides: "Updated forge_skill_md_sha256 hash"
      contains: "forge_skill_md_sha256"
  key_links:
    - from: "skills/forge/SKILL.md"
      to: ".forge.toml"
      via: "compaction defaults documentation"
      pattern: "token_threshold"
---

<objective>
Implement token optimization rules in skills/forge/SKILL.md — minimal task prompt construction, injection budget check, and documented .forge.toml compaction defaults.

Purpose: Keep Forge context lean by enforcing prompt size limits, selective injection, and validated compaction settings.
Output: Extended SKILL.md with token optimization section, updated plugin hash.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-agents-md-mentoring-and-token-optimization/03-CONTEXT.md
@.planning/forge-delegation-spec.md (section 10 — Token Budget Rules)
@skills/forge/SKILL.md (will have been extended by plan 03-01)
@.forge.toml (existing compaction defaults)
@.planning/phases/03-agents-md-mentoring-and-token-optimization/03-01-SUMMARY.md

<interfaces>
<!-- From .forge.toml — current validated defaults -->
max_tokens = 16384
[compact]
token_threshold = 80000
eviction_window = 0.20
retention_window = 6

<!-- From spec section 10 — token budget contract -->
Max task prompt: 2,000 tokens
Omit: full conversation history, unrelated file contents, verbose error logs
Include: only files directly relevant to task, current state of target files
Skill injection budget: only relevant skills, not all available
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append Token Optimization section to SKILL.md</name>
  <files>skills/forge/SKILL.md</files>
  <read_first>skills/forge/SKILL.md (full file after plan 03-01 extension), .planning/forge-delegation-spec.md (section 10), .forge.toml</read_first>
  <action>
Append a new section at the end of skills/forge/SKILL.md titled "## Token Optimization". Per D-07, D-08, D-09 from 03-CONTEXT.md, include:

### Minimal Task Prompt Construction (per D-07, TOKN-02)
Every task prompt to Forge MUST:
1. Use ONLY the 5 mandatory fields: OBJECTIVE, CONTEXT, DESIRED STATE, SUCCESS CRITERIA, INJECTED SKILLS
2. Stay within **2,000 tokens** maximum
3. CONTEXT field: include ONLY files directly relevant to the task (list paths + brief current state)
4. OMIT entirely: conversation history, unrelated file contents, verbose error logs, prior task outputs
5. If a task requires more context than fits in 2,000 tokens, split it into subtasks (each under budget)

### Injection Budget Check (per D-08, TOKN-03)
Before submitting any task prompt, verify:
1. INJECTED SKILLS field lists **at most 2 skills** unless the task clearly spans multiple domains (e.g., a security-sensitive test = security + testing-strategy)
2. If more than 2 skills would apply, prioritize by: security > task-specific > general quality
3. Research/read-only tasks: inject NO skills (empty INJECTED SKILLS field)

### .forge.toml Compaction Defaults (per D-09, TOKN-04)
The following values in `.forge.toml` are **tested defaults** that prevent Forge context bloat:

```toml
max_tokens = 16384          # Conservative output limit; raise if Forge truncates
[compact]
token_threshold = 80000     # Trigger compaction at 80k tokens (before hitting model limit)
eviction_window = 0.20      # Summarize oldest 20% of context on compaction
retention_window = 6        # Always keep 6 most recent messages intact
```

**Rationale:**
- `token_threshold = 80000`: Most models have 128k context; 80k triggers cleanup with headroom
- `eviction_window = 0.20`: Aggressive enough to free space, conservative enough to retain useful context
- `retention_window = 6`: Keeps current task chain intact (typical: prompt + 2 retries + outputs)
- `max_tokens = 16384`: Prevents single Forge response from consuming too much context

**User tuning:** If Forge frequently hits compaction mid-task, raise `token_threshold`. If context feels stale, lower `retention_window` to 4.
  </action>
  <verify>
    <automated>grep -c "Token Optimization" skills/forge/SKILL.md | grep -q "1" && grep -c "2,000 tokens" skills/forge/SKILL.md | grep -q "1" && grep -c "Injection Budget Check" skills/forge/SKILL.md | grep -q "1" && grep -c "token_threshold = 80000" skills/forge/SKILL.md | grep -q "1" && echo "PASS"</automated>
  </verify>
  <done>skills/forge/SKILL.md contains Token Optimization section with all 3 subsections</done>
  <acceptance_criteria>
- grep "Token Optimization" skills/forge/SKILL.md returns 1 match
- grep "2,000 tokens" skills/forge/SKILL.md returns at least 1 match
- grep "Injection Budget Check" skills/forge/SKILL.md returns 1 match
- grep "token_threshold = 80000" skills/forge/SKILL.md returns at least 1 match
- grep "eviction_window" skills/forge/SKILL.md returns at least 1 match
- grep "retention_window" skills/forge/SKILL.md returns at least 1 match
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update plugin.json integrity hash</name>
  <files>.claude-plugin/plugin.json</files>
  <read_first>.claude-plugin/plugin.json</read_first>
  <action>
Recalculate SHA-256 hash of skills/forge/SKILL.md after Token Optimization section was appended:
```bash
shasum -a 256 skills/forge/SKILL.md | awk '{print $1}'
```
Update the `forge_skill_md_sha256` field in `.claude-plugin/plugin.json` with the new hash value.

Then verify all 43 existing tests still pass:
```bash
bash tests/run_all.bash
```
  </action>
  <verify>
    <automated>NEW_HASH=$(shasum -a 256 skills/forge/SKILL.md | awk '{print $1}') && grep -q "$NEW_HASH" .claude-plugin/plugin.json && bash tests/run_all.bash | tail -1 | grep -q "PASS\|passed" && echo "PASS"</automated>
  </verify>
  <done>plugin.json hash matches final SKILL.md; all 43 tests pass</done>
  <acceptance_criteria>
- SHA-256 in plugin.json matches actual shasum of skills/forge/SKILL.md
- bash tests/run_all.bash exits 0 with all tests passing
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .forge.toml → Forge compaction | Config values control when Forge discards context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Denial of Service | Forge context overflow | mitigate | Compaction defaults documented with rationale; token budget enforced in SKILL.md |
| T-03-05 | Tampering | .forge.toml user edits | accept | User owns their config; documented defaults serve as reset baseline |
</threat_model>

<verification>
- All 43 existing tests pass: `bash tests/run_all.bash`
- skills/forge/SKILL.md has Token Optimization section with all subsections
- plugin.json hash is current
- No modifications to skills/forge.md or .forge.toml (read-only references)
</verification>

<success_criteria>
SKILL.md extended with token optimization rules. Task prompts constrained to 2,000 tokens. Injection budget limited to 2 skills. Compaction defaults documented with rationale. All tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/03-agents-md-mentoring-and-token-optimization/03-02-SUMMARY.md`
</output>
