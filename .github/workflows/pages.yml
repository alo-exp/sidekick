name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
    paths: [site/**]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/configure-pages@v5
      - name: Prepare Pages artifact (exclude markdown sources)
        run: bash scripts/prepare-pages-artifact.sh .pages-staging
      - uses: actions/upload-pages-artifact@v3
        with:
          path: .pages-staging
      - id: deployment
        uses: actions/deploy-pages@v4
