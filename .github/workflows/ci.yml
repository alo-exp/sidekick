name: CI
on:
  push:
    branches: [main]
    paths-ignore:
      - site/**
  pull_request:
    paths-ignore:
      - site/**
jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run test suite
        run: bash tests/run_all.bash
